# ESCALATION: run_deepforest_tree_crown needs a CPU Batch worker (Orchestrator seam)

The tools session BUILT + PROVED the registered-tool scaffolding (mirrors compute_canopy_height
exactly: it does NOT run the ML model inline -- it dispatches to a services/workers Batch worker
via run_solver). DeepForest = same shape (a PyTorch RetinaNet), so its inference CROSSES the tools
seam (services/workers = Orchestrator). The tool is HELD (NOT registered) -- it would always return
DEEPFOREST_WORKER_UNAVAILABLE until the worker lands, so registering it now would be a dead-end.

Scaffolding (proven, inert) is in this dir: run_deepforest_tree_crown.py + test_run_deepforest_tree_crown.py
(22/22 pass; py_compile OK; build_spec staged to S3 + run_solver('deepforest') correctly raised
SolverNotRegisteredError -> honest typed error, no fabricated layer).

## What the Orchestrator must provision (parallels services/workers/canopy)
DID NOT install deepforest into .venv-agent (charter: install only if clean+fast). pip dry-run showed it is HEAVY + CONFLICTING: pulls torch-2.12.1 + torchvision + pytorch-lightning + the FULL cuda-toolkit-13.0.2/nvidia-* GPU stack + transformers-5.12.1 + tensorboard, and would BUMP setuptools/huggingface_hub/opencv in the shared agent venv, plus a runtime HuggingFace weights download (GBs). This is exactly the canopy posture: heavy ML deps belong in the Batch worker image, not the agent box.\n\nARCHITECTURE DECISION (read compute_canopy_height.py first, per instructions): canopy does NOT run its CPU ML model inline -- it dispatches to a services/workers/canopy AWS Batch worker via run_solver('canopy'). I mirrored that exactly. DeepForest = same shape (a PyTorch RetinaNet), so its inference CROSSES the tools seam (services/workers = Orchestrator). I built ONLY the registered tool scaffolding: AtomicToolMetadata + @register_tool + bbox/param validation + AOI cap (0.06 deg^2, env-overridable GRACE2_DEEPFOREST_MAX_BBOX_DEG2) + NAIP-or-imagery_uri resolution + build_spec staging to S3 + run_solver('deepforest') dispatch + wait_for_completion + publish_layer(vector) -> FlatGeobuf LayerURI, with an honest typed DEEPFOREST_WORKER_UNAVAILABLE error (catches SolverNotRegisteredError) until the worker lands. estimate_payload_mb=1.0 (tiny vector). cacheable=False/live-no-cache/workflow_dispatch (mirrors canopy -- cache shim not invoked for solver dispatchers). No supports_global_query (AOI-scoped, capped).\n\n=== NEEDS-ESCALATION (ORCHESTRATOR, services/workers seam) ===\nTo activate this tool, the Orchestrator must provision the CPU Batch worker, exactly paralleling services/workers/canopy:\n1. services/workers/deepforest/ -- entrypoint.py + Dockerfile + __init__.py + test. Worker contract (I froze it in the build_spec): reads manifest build_spec {inputs:[{gs_uri,dest:'rgb.tif'}], build_spec:{input_file:'rgb.tif', output_file:'tree_crowns.fgb', patch_size, patch_overlap, iou_threshold, score_threshold, bbox?}, outputs:['tree_crowns.fgb','deepforest.stdout','deepforest.stderr']}; downloads rgb.tif; runs deepforest.main.deepforest().predict_tile(raster_path, patch_size, patch_overlap, iou_threshold); filters by score_threshold; georeferences the pixel-space boxes to lon/lat crown polygons; writes a FlatGeobuf named tree_crowns.fgb (cols score,label,geometry); uploads + writes completion.json in the SAME schema as canopy (status/exit_code/output_uris/started_at/finished_at/error; an empty crown set = honest error, NOT success). BAKE DeepForest weights into the image (no runtime HF download on the SPOT box, mirroring canopy's weights-permanence note). License MIT.\n2. solver.py SOLVER_WORKFLOW_REGISTRY += {'deepforest': 'aws-batch'} (CENTRAL file -- I did not touch it; I left a comment in the tool pointing here).\n3. infra: a deepforest job-def via tofu apply, then NATE flips GRACE2_AWS_BATCH_JOB_DEF_DEEPFOREST (the same inert-until-env-set gate SWMM/OpenQuake/deck-builder carried). Reuses the existing grace2-solvers Spot queue + run_solver/Batch/S3/completion.json seam (no new infra primitives).\n\nCENTRAL-FILE registration (I did NOT edit these -- main session unions): import_line above for tools/__init__.py; categories.py primary='conservation_ecology' + secondaries=('land_cover_development','hazard_modeling') (canopy uses primary hazard_modeling + the same two secondaries; I led with conservation since crown delineation is squarely ecological); corpus_queries above. The registration-coverage test will pass once unioned.

## When the worker lands -- register the tool (tools session or Orchestrator)
- move run_deepforest_tree_crown.py -> services/agent/src/grace2_agent/tools/
- move the test back -> services/agent/tests/
- __init__.py import: from . import run_deepforest_tree_crown  # noqa: E402,F401 -- tree-crown ML-inference tool: registers run_deepforest_tree_crown (DeepForest RetinaNet predict_tile on a NAIP/RGB COG; stages build_spec + dispatches run_solver('deepforest') CPU Batch worker + publishes per-tree crown bounding-box polygons as a FlatGeobuf vector). INERT: returns honest DEEPFOREST_WORKER_UNAVAILABLE until the services/workers/deepforest worker + solver-registry 'deepforest' entry + GRACE2_AWS_BATCH_JOB_DEF_DEEPFOREST land (Orchestrator seam).
- primary_category: conservation_ecology  secondary: ['land_cover_development', 'hazard_modeling']
- corpus_queries: [
 "count the individual trees in this small forest AOI",
 "outline each tree crown on the map for this neighborhood",
 "how many trees are in this stand of woodland",
 "detect and delineate individual tree canopies from the aerial imagery here",
 "run DeepForest tree-crown detection on the NAIP imagery for this bbox",
 "give me a per-tree bounding-box vector layer for this small preserve",
 "locate every tree in this ag-field edge so i can join it with canopy height",
 "map individual tree locations over this sub-metre RGB area"
]

## Proof
LOCAL-FIRST PROTO (real source): fetched a live tiny NAIP RGB COG for panhandle-FL AOI (-85.300,29.940,-85.297,29.943) -> s3://grace2-hazard-cache-226996537797/cache/static-30d/naip/e537a1bac6c580c6b7dc3ac6fa6b2c29.tif (the worker's input). Wrote+round-tripped 4 synthetic crown bounding-box polygons (score/label cols) to FlatGeobuf /tmp/run_deepforest_tree_crown_proto/tree_crowns.fgb (1968 bytes, valid read-back). LIVE END-TO-END through the inert-worker gate (real AWS, GRACE2_CACHE_BUCKET=grace2-hazard-cache-226996537797): NAIP fetched -> build_spec PutObject succeeded to S3 (s3://grace2-hazard-cache-226996537797/cache/static-30d/deepforest_setup/<ulid>/build_spec.json, 534 bytes, since cleaned up) -> run_solver('deepforest') correctly raised SolverNotRegisteredError (registry has canopy/geoclaw/landlab/modflow/openquake/sfincs/swan/swmm, no deepforest) -> tool returned honest typed DEEPFOREST_WORKER_UNAVAILABLE, NOT a fabricated layer. TESTS: services/agent/tests/test_run_deepforest_tree_crown.py 22/22 pass; canopy+deepforest together 41/41; py_compile OK; ASCII-clean.
