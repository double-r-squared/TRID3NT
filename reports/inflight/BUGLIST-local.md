# TRID3NT Local - living bug list
Updated: 2026-07-09 (Claude maintains; statuses: FIXED-VERIFIED / FIXED-UNVERIFIED / IN-PROGRESS / OPEN / WONTFIX)

| id | item | status | evidence |
|----|------|--------|----------|
| F1 | No mid-turn visibility (no cards/narration during turn) | FIXED-VERIFIED | Playwright 2026-07-09: thinking els at T+15s, cards 1->7 during turn; batch E2E C1 4 tool/running els mid-turn |
| F2 | Model selector generic / no hot-swap | FIXED-VERIFIED | live drive 2026-07-11: selector lists 9 real Ollama models; swap to qwen3.5-lowvram:9b-16k + back proven via Ollama /api/ps (ground truth of executing model); model_id rides user-message -> resolve_selected_model -> openai_model verbatim; proof docs/proof/58 |
| F3 | Layer stuck in loading loop | FIXED-VERIFIED | = unanswered payload gate; batch E2E C5: after gate click layer row appears, loading-els=0 |
| F4 | Landcover invisible | FIXED-VERIFIED | batch E2E: landcover prompt -> gate -> layer row rendered, no stuck loading |
| F5 | Auth surfaces in local build | FIXED-VERIFIED | Playwright: no gate, straight to app; Settings account section hidden |
| F6 | Gates time out locally | FIXED-VERIFIED | batch E2E C4: gate clicked at ~50s and still live (cloud would have expired); no-timeout 8deff81 |
| F8 | Thinking tokens stream (web, grey foldable, toggle) | FIXED-VERIFIED | e2e_thinking_stream.mjs 3/3 PASS: block streams (120 chars), answer follows, toggle collapses (visible true->false). Root cause found+fixed 2026-07-09: server.py never forwarded ThinkingDeltaEvent / never read payload show_thinking (F8 agents were cut by limits mid-work); wired dispatch loop -> agent-thinking-chunk. Proof docs/proof/44 |
| F9 | Thinking stream in QGIS dock | FIXED-VERIFIED | headless_thinking_proof.py vs LIVE agent: thinking=True answer=True at 18s, collapse works. Proof docs/proof/45 |
| F10 | No activity signifier between cards (running-tool spinner) | FIXED-VERIFIED | batch: running-tool card at dispatch start (pipeline-state running already fires; gap was unwired onAgentThinkingChunk + card); E2E C1 running els mid-turn |
| F11 | Payload gate missable (auto-scroll/pulse/mobile visibility + coarser option) | FIXED-VERIFIED | batch E2E C2 gate isVisible after force-scroll, C3 amber pulse border present |
| F12 | Client WS drop killed turn + misreported LLM_UNAVAILABLE | FIXED-VERIFIED | live drive 2026-07-11: mid-turn page reload kills transport -> server logs DETACHED (kept running) ~75ms later, earthquake layer arrives post-reconnect via replay backstop, zero LLM_UNAVAILABLE text; proof docs/proof/59 |
| F13 | Cases ephemeral per-device | FIXED-VERIFIED | batch E2E C6: second browser context sees the shared case list (single local user + adoption c93a809) |
| F14 | LAN/Tailscale access | FIXED-VERIFIED | http://100.92.163.46:5173 -> 200; all services bound; NATE reached it on phone |
| F15 | Mobile: spatial-input banner/toolbar/slider overlap (NATE live report 2026-07-09) | FIXED-VERIFIED | flex top-stack on mobile (overlap impossible by construction, structural test); desktop byte-identical, verified no overlap |
| F16 | Landcover over a state fails (guardrail dead-end + purpose='aoi' rejected + server rewrote purposes to 'barrier') | FIXED-VERIFIED | live E2E post-restart: WA prompt -> resolution card -> Proceed -> landcover layer renders, loading resolves. NLCD auto-coarsen (30-600m ladder, 4000px budget) + esri 10m tiled to 8 deg2 + aoi purpose end-to-end |
| OPEN-1 | Esri landcover fetch is 107MB (needs default coarsening/COG windowing) | FIXED | superseded by F16: NLCD auto-coarsen + esri tile/mosaic with honest payload estimate |
| F17 | QGIS: "case blank has no layers to export" when browsing cases (Open-in-QGIS was the export route) | FIXED-VERIFIED | Cases dialog rework: Open = primary (chat+layers), export renamed "Export GeoTIFFs", empty case -> friendly note; headless live proof 2026-07-09 |
| F18 | QGIS: white canvas on case select (layers added but no zoom + no basemap) | FIXED-VERIFIED | auto OSM basemap (settings-gated, default on) + zoom-to-case-bbox on open; deterministic dock-wiring proof: canvas lands on bbox |
| F19 | QGIS: no delete-case / new-case affordances | FIXED-VERIFIED | Delete (confirm) + New case in Cases dialog + header New button; live create/delete round-trip proven (case rebinds, deleted id vanishes from list) |
| F20 | QGIS: AOI checkboxes live-apply in dock; wanted in Settings with Save | FIXED-VERIFIED | both AOI toggles + auto-basemap moved to Settings w/ Save/Cancel, apply-at-once; dock shows read-only AOI status line |
| F21 | QGIS: yellow gate cards stay expanded after decision | FIXED-VERIFIED | collapse to amber one-line summary + "show details" read-only re-expand; verified under offscreen QGIS |
| F22 | QGIS: cases invisible until manual Connect (same class as cloud cases-box-off) | FIXED-VERIFIED | agent GET /api/case-list cold route + dock auto-connect on show (local mode) + cold list in Cases dialog + click-to-open queues select across connect; headless proof AUTO-CONNECT PASS |
| F23 | QGIS: every connect minted a throwaway "QGIS session" case (clutter regrowth) | FIXED-VERIFIED | connect reuses resumed/newest case, creates only at zero cases; proof NO-NEW-CASE 188->188; case cleanup 2026-07-09: 158 zero-layer junk cases soft-deleted (345->188 active, user-approved) |
| F24 | Duplicate resolution gates per model retry hang the turn (model arg-fumble -> new gate per retry; 2nd gate unanswered forever since local gates never expire) | FIXED-VERIFIED | per-turn gate decision memory (tool+bbox, proceed/narrow_scope only, delta-merge revised args); mobile drive rerun: single gate -> Confirm -> layer at T+24s (M4 PASS); 214 targeted tests green |
| F25 | fetch_landcover rejects dataset='nlcd' (model's natural first guess) -> 2 wasted error retries per turn | FIXED-VERIFIED | 'nlcd'/'nlcd_' alias to default vintage nlcd_2021; live rerun had zero dataset retries; 21 landcover tests green |
| F26 | State-scale NLCD renders ocean as OPAQUE BLACK (NOT our resample: MRLC WCS source colormap maps class 0 'Background' to opaque black; GDAL forbids alpha on non-nodata palette entries) | FIXED-VERIFIED | fix = fold 0 -> transparent nodata(255) at ingestion + cache v3; live MinIO-path proof: fresh WA COG zero_pixels=0, 969k px remapped, nodata alpha=0; visual docs/proof/48; 305 targeted tests |
| F27 | QGIS plugin rasters NEVER painted (XYZ layer valid but zero tile requests - qgis_xyz_uri fully percent-encoded the template and this QGIS build does not decode the url component; OSM basemap affected too) | FIXED-VERIFIED | NATE live report 2026-07-10; request-logging stub proved 0 requests -> minimal encoding (only query & -> %26) -> 24 requests + landcover paints (13801 samples vs 0), proof docs/proof/49; 126 tests; plugin reinstalled |
| F28 | QGIS: layers compound/mix across case switches (previous case groups never removed) | FIXED-VERIFIED | clear all TRID3NT groups except the new case's on every case-open (basemap + user layers untouched); live proof: one group survives per switch |
| F29 | QGIS: dock chat kept previous case's messages on switch | FIXED-VERIFIED | clear + replay session_state.chat_history (last 50, user/agent rows) per case-open; live proof 25-msg replay |
| F30 | QGIS: time-series rasters flat, animation undiscoverable | FIXED-VERIFIED | frame sequences grouped into collapsed "(animation, N frames)" subgroups + Temporal Controller hint note; SIP node-relocation crash found+avoided (insert-at-construction); 7-frame live proof |
| F31 | QGIS: no auto-focus moving between cases | FIXED-VERIFIED | zoom hardened: case bbox -> vector union -> case-list bbox fallback, honest "no stored map area" note otherwise; live proof extent change + note |
| F32 | Case bbox never persisted (cloud gap too: only SOLVERS pinned AOI; fetch-only cases got bbox None) -> no anchor in Case-state note -> model guesses bbox (2m43s spiral); + per-connection handle registry advertised-but-unresolvable after reconnect | FIXED-VERIFIED | durable bbox pin on bbox-carrying fetches + registry seed/replace on case-switch + DEM-aware error hints; 152 old cases backfilled from COG bounds; live acceptance: same prompt -> fetch_dem with the LITERAL anchor verbatim at T+1m41s |
| F33 | fetch_dem hard 10000 km^2 guardrail dead-ends state-scale terrain asks (WA=230638 km^2) even with a correct bbox - same class F16 fixed for landcover | FIXED-VERIFIED | pixel-budget coarsen (4000px, area-weighted) + 5M km^2 ceiling + ladder to 900m; live: WA -> 150m DEM 2min -> hillshade published + tile renders (proof 51/52); 200 targeted tests |
| OPEN-9 | Published layers got bare-ULID names when the model omitted layer_id/name | FIXED-VERIFIED | derive_readable_layer_name (explicit name > style_preset label > uri segment > generic+disambiguator); explicit workflow names untouched (flood frames still 'Flood depth step N' live) |
| F35 | GeoClaw local image missing (docker exit 125 before solver start; trid3nt-local/geoclaw:latest pruned at some point) + install.md build one-liner had wrong context | FIXED-VERIFIED | image rebuilt (1.08GB, vendor/ context), doc fixed; geoclaw solve status=complete ~2min; drives PASS on web AND QGIS dock (fresh case, inundation frames in layer tree at 118s) |
| OPEN-8 | case-list emission storm (server re-emitted ~190 cases on EVERY keepalive session-resume, no change check; 3595 emissions in 12h on one session; plugin audited clean) | FIXED-VERIFIED | per-session content-digest guard (force on explicit resume/mutations, cleared on disconnect); post-restart hour: 21 emissions incl. purge churn vs ~1/s before |
| OPEN-11 | Tile-404 noise: MapLibre requests viewport tiles outside COG bounds, TiTiler 404s each (1006 in one passing flood run) - harmless but log spam + wasted requests | OPEN | fix = set MapLibre raster source bounds from layer bbox |
| F34 | QGIS: prompt after "New case" ran + persisted into the PREVIOUS case (case_command('create') never updated the wire client's case_id stamp; dock showed layers live but they filed to the old case = "case looks empty" on reopen) | FIXED-VERIFIED | pump adopts every server case-open rebind into client.case_id; NATE-requested acceptance: fresh flood case owns bbox + 8 layers, switch to WA case + back = 11/11 (name-collision false positive excluded), rendered-map proofs 54-s1/s2/s3; 145 tests; plugin repo updated |
| OPEN-10 | geocode 'downtown <city>' -> single-building AOI (root cause: 'Downtown Tampa' is an 11m TRAM STOP node in OSM, no neighbourhood polygon exists - class preference alone could never fix it) | FIXED-VERIFIED | Nominatim limit 1->5 + place-class promotion (POI queries untouched) + 2km AOI floor w/ expansion_note; live before/after 11m -> 2.0km; 75 targeted tests |
| OPEN-12 | e2e_flood_proof summary measured the PRE-run panel (only passed when the picked case already had old depth layers; every fails-after-restart/passes-on-rerun pair explained) | FIXED-VERIFIED | post-run scan w/ 150s depth-row wait (trid3nt-local 7e5c245); fixed-driver smoke: depth=true groups=1 on a genuinely fresh run. OPEN-13 (verbatim-prompt autonames) still open |
| OPEN-14 | Context-ceiling silent truncation -> FABRICATED success: turn-2 prompt hit exactly num_ctx=16384 (Ollama silently truncates), the 9b model lost its tool contract, emitted zero tool calls yet narrated 'I have computed the hillshade... and published' (reproduced 2x; honesty-floor class - nothing guards chat-text claims with no tool calls) | FIXED-VERIFIED | context_budget.py: proactive compaction ladder (drop narration -> harden tool results + cap oversized text parts -> fold digest -> loud step d) under budget=num_ctx-4096-1024, reactive clip guard (recompact 60% + retry once -> typed CONTEXT_WINDOW_EXCEEDED honest abort), fabrication backstop caveat on zero-tool-call action claims, max_tokens=4096 cap, compaction pipeline card in chat (tool_name context:compact, persists). Ladder b/c-never-fired bug (falsy empty working list + function_response-only harden missing mixed narration+call rows) fixed 187ea15 + unconditional 8000c per-row normalize; 153 tests; flood smoke depth=true groups=1 on honest driver 2026-07-12 |
| OPEN-15 | Tool schemas + system prompt alone are ~15.7k tokens of the 16,384 num_ctx, so content compaction can bottom out (plume case: 6k->1k content, whole-prompt 22107->16680 vs budget 11264) and every turn on such a case honestly aborts CONTEXT_WINDOW_EXCEEDED - compaction cards mint/complete/persist correctly, but the turn can never fit | OPEN | mitigations: bigger-num_ctx model variant (24k/32k qwen3 tag), or tool-RAG top-k retrieval (deferred task #162, this IS the offline build it was scoped for); found during compaction-card live proof 2026-07-12 |
| OPEN-16 | Aborted-turn persist logs "TypeError: Object of type datetime is not JSON serializable" traceback adjacent to the terminal-failure persist (pre-existing, log noise; persist still lands) | OPEN | json.dumps default=str or isoformat at the persist seam |
| OPEN-2 | Styled exports: 0-transparency only when ramp vmin==0 exactly | OPEN | noted in styled-export lane |
| OPEN-3 | QGIS remote mode: exports do not download GeoTIFFs | OPEN | pre-existing, noted |
| OPEN-4 | 22 unusable tools list (post-remediation re-measure pending) | OPEN | usability re-run not yet scheduled |
| OPEN-5 | Local model first token ~75s cold (qwen3 unload after idle) | FIXED-VERIFIED | keep_alive=24h warmup baked into start_agent.sh (fires on every agent start, non-fatal) + manual pin; observed done_reason=load on boot |
| OPEN-6 | Old flood case replay: depth layers absent + tile 404s on the pre-existing Chattanooga case (stale MinIO artifacts from old runs suspected, possibly pruned) | OPEN | e2e_flood_proof 2026-07-09: first run has_depth_layers=false; SECOND run (post landcover-fix restart) has_depth_layers=true + animation group - may be intermittent or case-pick dependent, downgraded to flaky |

| OPEN-7 | Test-suite hygiene: test_active_aoi_repair_job2::test_bare_followup_refetch_short_circuits (5-min solver-confirm gate timeout for fetch_dem, then FAIL - fetch_dem swept into a confirm gate the test never answers) + test_modflow_archetype_offload.py collection error (imports 'services', needs repo-root run; from offload commit 46b08c7) | OPEN | both PRE-EXISTING: reproduced identically on pre-change commit d59b402 in a clean worktree |

Drive log 2026-07-09 (Claude self-drive, NATE-requested):
- QGIS headless drive 10/11: auto-connect, no-new-case, 188-row dialog, click-open (18 layers + OSM + zoom to Chattanooga), New case, Delete, county landcover layer landed gate-free (small AOI, correct). Miss: no thinking text observed that turn (seam proven elsewhere same hour - web M1 at 3s/38s; qwen3 emitted no reasoning on that tool turn; not a code bug).
- Mobile drive (iPhone 13 viewport) 6/7: thinking, gate renders + fits viewport (364x310), Confirm tap works, no stuck loading, no chrome overlaps. Miss = F24 (duplicate gate hang). Driver kept: web/tools/e2e_mobile_landcover_drive.mjs. Proofs: docs/proof/46-*, 47-*.

Notes 2026-07-09 (AFK batch close-out):
- F8 was the real find of the night: the 2026-07-08 F8 commit shipped adapter+contract+UI but the server dispatch loop hooks were missing (both building agents were cut by session limits mid-work and the note said UNVERIFIED). Symptom was thinking-els=0 in the batch E2E; fixed in server.py (payload show_thinking -> stream_events_with_contents; ThinkingDeltaEvent -> agent-thinking-chunk, gated on the per-turn toggle, bubble-id shared with the answer).
- Verification drivers kept: web/tools/e2e_thinking_stream.mjs, web/tools/e2e_f9f10_gate_thinking.mjs, qgis-plugin/tests/headless_thinking_proof.py.
