# TRID3NT Local - deferred backlog (do when we don't have much else running)

Slow-day / parallel-when-idle items NATE has explicitly deferred. Not bugs (those live in BUGLIST-local.md), not the active build. Pull from here when a lane is free.

| id | item | why deferred | detail |
|----|------|--------------|--------|
| BK-1 | Base maps in Settings + fresh-slate startup (QGIS plugin) | NATE 2026-07-14: "do TELEMAC first, this can come later when we don't have much to do" | TWO linked UX changes, match the cloud. (a) FRESH SLATE ON STARTUP: stop auto-opening the last case on connect (that auto-open is what leaked the Chattanooga AOI, F46); boot to just the map / no case, and the FIRST chat message auto-creates a case (cloud behavior). (b) BASE-MAP SELECTOR in Settings (apply-on-Save, item-4 pattern like the AOI/thinking toggles): dropdown of XYZ presets - ESRI World Imagery (satellite), CartoDB Dark Matter (dark), OSM (current default) - plus an optional STATE-LINES overlay (TIGER). Self-contained via QGIS native XYZ tiles (our dock already adds OSM that way in ensure_basemap) - NO external plugin dependency needed (evaluated QuickMapServices; XYZ presets are simpler + offline-friendlier; re-confirm QMS isn't strictly better when starting). After it works, re-run the groundwater/dye demo for reproducibility on the improved base maps. |

| BK-2 (FIXED 2026-07-16) | TELEMAC river-dye: local-model routing reliability - ROOT CAUSE was tool-RETRIEVAL not model choice: enforce K=8 + run_telemac had NO corpus queries so it was never a candidate. Fixed by adding spill-scenario queries to tool_query_corpus.yaml (882f9bd); 6/6 realistic prompts now retrieve it. Ready prompts: reports/inflight/telemac-dye-test-prompts.md | 2026-07-14: engine built + code-complete (P0-P4), but local qwen3:8b mis-routes the natural "dye spill in the river" prompt to the GROUNDWATER seepage tools ~part of the time at temp 0.7 (they sound alike) | Engine is invocation-correct + flood-smoke-green + tool registered (194 tools). Fixes already applied (sharper surface-vs-subsurface descriptions + river_geometry_uri passthrough + "use run_telemac" deferrals in both seepage tools) -> post-fetch routing probe now 5/5 run_telemac, but the live post-geocode first decision is still stochastic. TO CLOSE: lower local routing temperature (openai_adapter.py temperature 0.7 -> ~0.2 for tool selection) OR add a routing nudge OR ship tool-RAG (project_tool_retrieval_rag_for_local_models, the real fix at 194 tools). Full acceptance driver: qgis-plugin/tests/headless_telemac_p4_acceptance.py. Render/animation half already proven (P3, telemac_p3_plugin_dye.png). |

| BK-3 | TELEMAC mesh refinement = LLM tools + user-editable/pushable mesh layer + satellite basemap (NATE 2026-07-17, motivated by OPEN-26: auto-nav picked the wrong watercourse) | active feature ask | FOUR parts: (a) SATELLITE basemap preset in Settings (ties BK-1 base maps) so the user sees the real river; (b) the transport/dye COG styled with ZERO->transparent so the basemap shows through (render bug: griddata onto the convex hull smears dye across land - postprocess must color only channel/mesh cells, not the hull); (c) LLM mesh tools: regenerate mesh at coarse/med/fine resolution + adaptive (coarse straight, fine bends+release) [adaptive-mesh-budget pattern, TELEMAC = the 2nd unstructured-mesh engine that triggers it] + select/snap the correct flowline (fixes OPEN-26); (d) the mesh (or its centerline) is a PUSHABLE + EDITABLE layer - user draws/edits the real channel in QGIS, pushes it back, worker re-meshes + re-solves. The .slf mesh already loads as a native MDAL layer (feature #156), so 'view mesh' exists; this adds edit->push->re-solve + quality readout (nodes/elems/edge-len/min-angle). |

| BK-3c | TELEMAC mesh GRANULARITY KNOB - resolution is a USER/LLM lever, never hardcoded (NATE 2026-07-17) | SHIPPED 2026-07-17 (autoscaler+CFL dt+ladder live in the approve-mesh gate; commits e428ac3+3a4f58c+179297f+698434b) | Today the worker hardcodes ~14 m edge length (telemac_river_dye_build.py). WRONG per [[feedback_user_controlled_granularity]] + the shipped #154 granularity gate: expose mesh resolution as (a) a TOOL PARAM the LLM sets from prompt language ("high-res mesh", "coarse for speed"); (b) an AUTOSCALER SUGGESTION from AOI size / reach length (small AOI -> fine edges, large -> coarse, under an element/node CAP = the adaptive-mesh-budget pattern, TELEMAC = its 2nd-engine trigger); (c) the DEFAULT is the autoscaler suggestion, ALWAYS overridable. CRITICAL UX: this knob is WHAT the "approve this mesh" gate (BK-3b) surfaces - the gate shows resolution + node/element count + estimated solve time as EDITABLE controls (mirror the flood granularity card, gate.py), so approving the mesh = seeing+tuning the details, NOT a black-box yes/no. Answers NATE's worry that approve-mesh could hide controls worth having. |
| BK-3b | TELEMAC pre-sim QUALITY GATES (NATE 2026-07-17): sims are slow + a bad mesh ruins them, so gate quality BEFORE the solve | SHIPPED 2026-07-17: mesh-only worker preview (8-40s) + wireframe layer BEFORE the card + approve-mesh gate w/ REAL npoin/dt/est + ladder; proven live thru proceed-pin; final solve->publish leg pending one clean run (upstream DEM 503 blocked the witness). Driver: tests/headless_bk3b_approve_mesh_drive.py (stub-validated) | (a) MESH PREVIEW: split mesh-build from solve - build+return the mesh first, render it (over satellite, per BK-3), so the user sees it before committing compute; (b) "APPROVE THIS MESH" gate card - reuse the existing GateCard/payload-warning confirm pattern (gate.py / model_river_dye composer emits a payload-warning the dock renders); user approves / edits / cancels before the expensive TELEMAC run; (c) ties OPEN-26 (user catches the wrong-channel mesh here instead of after a 20min wasted solve). |
| BK-4 | GENERIC reusable spatial-input in the QGIS PLUGIN (NATE 2026-07-17) | active | Three linked, all GENERIC (reusable across cases/engines): (a) POINT/PIN input gate - user prompted to drop a pin (or one pre-placed) that MEANS different things per case (spill point here, well/sample/gauge elsewhere); the agent request_spatial_input + tools/spatial_input_tool.py already exist (web uses them via terra-draw); the PLUGIN just needs a QgsMapTool point-draw surface + a gate card to answer them. (b) DRAW AOI on new case - web has it (terra-draw, tasks 129-132); the PLUGIN does NOT (AOI = canvas extent via Settings toggle only). Add a QgsMapTool polygon/box draw on New-case; ties #170 AOI-first. (c) SHOW AOI as a dotted box/polygon overlay via QgsRubberBand (buildable; not shown today). Answer to NATE's Qs: dotted-AOI-box = buildable, not present; draw-AOI = web yes / plugin no, buildable on the existing agent spatial-input seam. |
| BK-5 | Evaluate the OpenQuake QGIS plugin as an abstraction layer over our hand-rolled OpenQuake system (NATE 2026-07-17, found while browsing QGIS plugins) | DEFER - not related to current TELEMAC work | The GEM "OpenQuake Integrated Risk Modelling Toolkit" (IRMT) QGIS plugin wraps the OQ engine + risk/exposure workflows. Idea: possibly COLLAPSE the complexity of our current OpenQuake tool chain (run_seismic_hazard_psha + fault fetcher + composers) onto the more abstracted plugin surface. Assess: (1) what it actually abstracts (job builder, exposure/vulnerability, WebUI/engine-server calls) vs what we hand-rolled; (2) whether it fits our seam model (agent-driven tools, local-docker/Batch solve) or assumes the OQ WebUI server; (3) net simplification vs new dependency. Do NOT start until the TELEMAC mesh track + reproducibility lanes clear. |

## Accumulated observations to fold in when the relevant lane runs (from live drives, not yet scheduled)
- MODFLOW/tracer concentration is unphysical + model-dependent (~16-31 g/L; swings 1000x with the model-chosen release_rate 0.1 vs 100 kg/s). Needs a sanity clamp/normalization. [groundwater realism lane]
- MODFLOW transport (run_modflow_job) rebuilds its own flow field instead of reusing a prior pumping-well flow field in the same case - wasteful double-solve + plume doesn't feel the well's capture (part of why the GW plume renders as a straight pencil). [groundwater realism lane]
- No-follow-up-offers system line (F41) still leaks on SOME turns (e.g. a T1 "Would you like to proceed with publishing...") - not fully effective; harden. [agent prompt lane]
- MODFLOW/GWT plume time series exists in modflow_mesh.nc (366 steps) but is only surfaced as a single peak raster on the map - not played as an animation. [viz lane]
