# UX feedback — batch 1 (post flood→Pelicun manual test)

**Source:** NATE ALMANZA live test of facet A (flood → Pelicun), 2026-06-16.
**Process:** accumulate → sequence carefully (cross-panel regression risk) → land a set → re-demo → next batch → coalesce overlaps. This file is the living accumulation for batch 1.

## Positives (no action — keep)
Flood sim ran clean; AOI bbox visible during the run; Case persistence solid (chat + layers rehydrate); Case auto-naming works well.

## Captured items
| # | Item | Area | Type |
|---|---|---|---|
| F1 | Pelicun ImpactPanel flashed then was unrecoverable after navigating away — must **persist** | viz/report | bug+feature |
| F2 | Persist the Pelicun **report** as a selectable **stub with preview**; opens a **full-screen popup overlay** to read fully (like the one-off graph pattern, but it's a report) | viz/report | feature |
| F3 | New **"Visualizations / Reports" section** under Layers — list of graphs/reports/supplementary visuals, selectable | viz panel | feature |
| F4 | **Drop in-chat visualizations**; route all graphs/reports to the new section (approach markdown-in-chat carefully so nothing breaks) | viz panel | feature |
| F5 | Viz items: **named + timestamped + "NEW" badge** until viewed | viz panel | feature |
| F6 | Viz section **collapsible** | viz panel | feature |
| F7 | 2nd layer name unreadable/truncated (`fort-myers-100yr-flood-depth…`); only flood-depth visible — naming/readability | layers panel | bug |
| F8 | **Opacity slider bug**: thumb starts centered but labeled 0% (value↔position mismatch; center reads 49%) | layers panel | bug |
| F9 | Layers list must become **scrollable** as layers accumulate | layers panel | feature |
| F10 | **Resizable chat**: drag the chat's **left border** to size it; **drop the large/normal expansion button** | layout | feature |
| F11 | **Resizable Layers panel**: drag its **right border** | layout | feature |
| F12 | Move **Sign out** into the **Settings** page | settings | bug |
| F13 | **Legend/key bundled with its layer**, shown at bottom of the AOI; **hides when the layer hides** (multi-key stacking: defer — use top-of-stack layer's key) | layers/map | feature |
| F14 | **AOI bbox persists after exiting the Case** (client-state not reset on Case exit) | client state | bug |
| F15 | **No emojis** in agent output | agent | bug |
| F16 | **Payload warning → opaque banner "hat"** pinned directly above the chat window (out of the scroll so you can't scroll away from it); same contents, not transparent; dismisses on feedback. Drop the top-of-chat overlay placement | chat/layout | feature |
| F17 | **Follow-ups in an existing Case recompute from scratch** — asked for a hillshade in Fort Myers, it re-ran the flood. ROOT CAUSE: on case-open the server resets the LLM conversation (`state.chat_history = []`, job-0245 cross-CASE fix) and re-seeds only layers + URI registry, NOT the model's memory of prior work. Fix: rehydrate the LLM history (text transcript) from the persisted PER-CASE messages on case-open (inherently case-correct → no job-0245 regression) + optionally inject a compact "layers already present" list | agent harness | bug (significant) |
| F18 | **New tool cards render behind the last prompt** instead of at the end of chat — the work-in-progress card sits above the latest user message, breaking the sense of continuity. New tool cards must append after the most recent prompt | chat ordering | bug |
| F19 | (refines F1-F3) Impact **report belongs in the new graphs/reports section in the BOTTOM HALF of the LEFT panel** (with the layers), and the popup opens **CENTERED in the browser window**; re-accessible from that section | viz/report | feature |
| F20 | **Follow-ups should reuse the original run's AOI bbox** for a cohesive picture (for data sources that support extent). Ties to F17: if the LLM remembered the run it would reuse the AOI. Mechanism: carry the Case's AOI bbox into the LLM context + nudge tools to reuse it | agent harness | feature |
| F21 | **Hillshade extent spills just outside the AOI bbox.** CORRECTED (NATE 2026-06-16): orientation is PERFECT — it aligns to the flood + basemap, NOT sideways. The only issue is the rendered area is slightly larger than the AOI: compute_hillshade ran on the full fetched DEM tile rather than being clipped to the AOI bbox/polygon first. Fix = clip DEM (or hillshade output) to the AOI before publish; ties to F20 (reuse AOI). Low urgency | engine/agent | bug (clip-to-AOI) |
| F22 | (refines F7/F8) **"Connected sliders"** — the recompute created a 2nd layer with the SAME NAME, and the opacity sliders moved together (keyed by name, not layer_id). J3 must key controls by layer_id; F17 fix reduces duplicate creation | layers panel | bug |
| F23 | **DEFERRED (later sprint):** "AOI-only mode" — show only the AOI extent, drop the basemap, swap in something like a land-cover backdrop. Should compose easily now that experiments are bbox-bound. Do NOT build now (don't inundate the in-flight batch) | map/viz | feature (deferred) |
| F24 | **DEFERRED (→ sprint-16 QGIS substrate):** layer **blending modes** exposed in the layer section / to the AI (QGIS Server WMS gives these free). Flagship: blend road overlay + flood map so submerged-road segments render RED while dry roads keep their color — precision opacity can't give. This is a core "why QGIS Server" capability. SRS-amendment-worthy | layers/render | feature (deferred) |
| F25 | **DEFERRED (→ post sprint-16):** **shelter/evacuation routing** — hazard-AGNOSTIC routing to shelters/hospitals/schools that flags roads likely to close (from blended flood+road data) vs clear, plots viable routes, surfaces bottlenecks. Uses QGIS network-analysis algorithms over the blended hazard data. Reusable across disaster types. SRS-amendment-worthy (new flagship use case) | engine/routing | feature (deferred) |

## Round-3 additions (2026-06-16, post-deploy)
- **F26a (BUG — case routing): retry message created a NEW Case instead of continuing.** After a gated execution timed out, NATE typed a retry into chat and it spawned a NEW Case rather than continuing the active one — i.e. he was dropped out of the active Case (so the message routed to Cases-root, which auto-creates a Case per the per-Case-stream design). Likely cause: the timeout/error reset client active-case state (same client-state-reset theme as [[feedback_wave48_known_bugs]]). Investigate: does a gate-timeout/error clear activeCaseId? Should NOT. Candidate for this batch or next.
- **F26b (FEATURE, NEXT SPRINT per user): retry mechanic + natural-language retry.** Want both (1) an explicit "retry this timed-out run and continue" control on the gate/card, AND (2) the ability to just ASK the agent to retry in chat and have it re-run the failed/timed-out step. Today: tool errors feed back to the agent (tool-retry-on-failure) and with J8 live re-sending is cheap (no recompute), but there's no first-class retry affordance. Need the repro (which gate/engine; gate-expiry vs solver-timeout). [[feedback_tool_retry_on_failure]]

- **F27 (gate UX): per-Case "Don't ask again" on the confirmation/payload gate.** The gate fires on every map-making run, interrupting AFK work. Want: keep "Proceed anyway" AND add a "Don't ask me again" button that sets a PER-CASE suppress flag (so the user controls where gating is on/off). Near-term = coarse per-Case boolean; fine-grained (per-tool / size-threshold / time-boxed) DEFERRED per user. Applies to the payload-warning gate and/or solver-confirm gate — confirm which the user means (the >25MB payload warning vs the solver confirm card). Good fit alongside J7 (the banner) since both touch the gate UI.

- **F28 (BUG): AOI bbox no longer visible per-Case (Fort Myers).** Surfaced right after the ux-batch-1 deploy. Likely cause: J5's case-open `clear-analysis-extent` was too broad — it cleared the extent whenever there was no Case bbox AND no replayable zoom-to, which can wipe a legitimate AOI. FIX (committed, pending deploy): only clear when the Case ALSO has zero loaded layers (a Case with layers keeps its AOI). NOTE: this stops the WIPING; if the AOI still doesn't DRAW on reload, the deeper cause is the zoom-to replay not resolving (agent persisting `map_command_emissions`, or `case.bbox` null) — needs a box-data check + possibly deriving the AOI from a stored Case bbox. [[feedback_wave48_known_bugs]]
- **FLOOD-DEPTH HANDLE (BUG, live, headline-blocking): SFINCS→Pelicun chain fails on Chehalis WA** — `PELICUN_RUNTIME_ERROR: local path does not exist` for the flood-depth handle; COG not resolving to S3. Flood/hillshade/roads/NSI all succeed; only Pelicun/impact blocked. My deploy is clean (only adapter.py+server.py; flood/publish/storage code intact on box per grep). Likely novel-area or storage-path issue (cf job-0304/0305/0306/0307). Opus read-only diagnostic dispatched to root-cause on the box.

## POST-DEPLOY INCIDENT (2026-06-16, P0 — under live diagnosis)
- **F-RENDER (P0): layers in the panel but NOT on the map, GLOBAL (Fort Myers too).** My web render code + build config are byte-identical to the last-known-good (Map.tsx render path untouched; .env.production.local already had the CloudFront vars), and /cog is up + the agent emits valid templates — so the suspect is a RUNTIME failure in the deployed Map.tsx command/render path, OR the agent restart. Live Playwright repro dispatched (signs in, publishes a layer, inspects MapLibre sources/layers + console + network). HOLD all web changes until it returns; fix the real cause, one corrected deploy.
- **F-AOI-BLEED (likely same root as F-RENDER): switching Fort Myers → Chehalis shows Fort Myers' bbox in Chehalis** (stale extent not cleared; Chehalis draws no AOI). Both AOI + layers route through Map.tsx — if its command/render handling is broken at runtime, the clear-analysis-extent + zoom-to-replace + layer-add all fail together. NOTE: my F28 conditional-clear (committed, NOT deployed) is WRONG for this bleed case (it stops clearing when the case has layers) — must be redesigned to ALWAYS clear-then-draw-own on case-open. Do NOT deploy F28 as-is.
- **F-CASES-CLEAR-ALL: navigating to the Cases root must clear ALL layers AND the bbox.** Currently the extent (and possibly layers) can linger. Part of the same client-state-reset family.
- **F-ROADS-TITILER (publish bug): a vector (roads .fgb) was published through the TiTiler /cog RASTER route** (template=.../cog/tiles/...png?url=...fgb). Vectors must render via inline GeoJSON (job-0175) or QGIS Server WMS, never a TiTiler COG template. Fix publish_layer's vector branch. (Roads also carry inline_geojson, so this may be cosmetic-but-wrong rather than the render blocker.)
- **TiTiler-vs-QGIS (answered, not a bug):** TiTiler stays as the single-band COG raster fast path (flood/hillshade — preserves the look); QGIS Server is additive (vectors + styled/layout + compute substrate) and is sprint-16 (not on AWS yet). [[project_qgis_processing_as_agentic_compute_substrate]]

- **F-MONGO-CLEANUP (low): remove/disable the `mongo_query` tool.** MongoDB was torn down 2026-06-16; persistence is DynamoDB. The tool is still registered (89-tool list) and would error if the LLM ever calls it. Drop it from the registry/catalog. [[project_aws_deploy_facts]]

## NEXT BATCH (post render-incident, ordered)
1. NATE live-tests the render fix (layers render light+dark; AOI bleed gone). HE does this.
2. **Flood honest-failure (agent):** when the flood workflow aborts (e.g. Chehalis outside NOAA Atlas 14), return a clear ERROR (no fabricated flood-depth handle) so the agent narrates honestly instead of hallucinating a handle → opaque Pelicun error. Also defense-in-depth: uri_registry should raise URI_HANDLE_UNRESOLVED for handle-shaped strings that match nothing (not fail-open to a local path). (Started reading model_flood_scenario.py:680 _build_failed_envelope.)
3. **PNW precip coverage (engine):** NOAA Atlas 2 / regional IDF fallback so Pacific-NW (Chehalis) floods actually model. Bigger.
4. **J2** (Visualizations/Reports panel + persisted centered Pelicun report popup — the headline UX item, F1-F6/F19).
5. **J3b** (F7 layer-name readability + F13 legend-with-layer), **F27** (per-Case "don't ask again" on BOTH gates), **F26a** (new-case-on-retry bug), **F-ROADS-TITILER** (vector through /cog), light→dark flash polish, **F-MONGO-CLEANUP**.

- **F-LONGRUN-SURVIVE (BIG, architecture): long-running solvers are cancelled on WebSocket disconnect.** Live-confirmed 2026-06-16: a New Orleans SFINCS run (6m37s in, docker healthy) was killed when the browser dropped the WS (`received 1001 going away`) — i.e. a hard-refresh OR navigating out of the Case mid-run cancels the solver (`wait_for_completion CANCELLED` -> `docker kill` -> `model_flood_scenario cancelled while awaiting solver`). The model did NOT fail; it was interrupted. This is the per-Case-resume requirement: leaving a Case (or refreshing, or a network blip) must NOT kill the running sim — the solver should run to completion server-side, progress persisted, and the client re-attach on return. Decouple the turn/solver lifecycle from the WS connection. Likely explains several earlier "flood failed" moments this session (refreshes/navigation during render-debugging were cancelling solvers). RENDER INCIDENT is now CLOSED (user confirmed bbox + version hash 43b1df0 render on 43b1df0). Distinct from: Chehalis (Atlas-14 coverage gap), Seattle (flood workflow returned empty handle — diagnose).

- **F-CASE-INTEGRITY (BUG, tied to F-LONGRUN-SURVIVE): mid-turn disconnect corrupts Case persistence + binding.** Live 2026-06-16: New Orleans Case "did not save" AND its chat surfaced under the FORT MYERS Case. Almost certainly the same mid-turn WS drop — the New Orleans turn was cancelled before its chat/Case persisted, and on reconnect the stream re-bound to the previously-active Case (Fort Myers) → cross-Case chat contamination (job-0266 per-Case streams / job-0268 case-binding territory). Fixes: (a) persist the user turn + Case on submit, BEFORE the long tool run, so a cancellation can't lose it; (b) make reconnect re-bind to the CORRECT owning Case (envelope case_id), never the stale active one; (c) part of the same disconnect-robustness block as F-LONGRUN-SURVIVE. NEXT BLOCK = agent disconnect/Case-lifecycle robustness (decouple solver from WS + persist-early + correct re-bind). Diagnose carefully (routing/persistence sensitive → adversarial-verify gate).

- **F-DATA-FALLBACK-NORM (design rule, user 2026-06-16):** every data source must degrade gracefully — primary → secondary fallback → HONEST typed error; never a silent dead-end or success-shaped empty result. First: NOAA Atlas 14 → Atlas 2 (PNW). SRS-amendment-worthy (FR-DT resilience). [[feedback_data_source_fallback_norm]]
- **F-TOOLCARD-EXPAND (PROMOTED from deferred, user re-requested 2026-06-16):** chevron on each tool usage card → expands to `input:` (args) + `output:` (raw function_response), Claude-Code-style, collapsible back to the compact card. Doubles as a live correctness check (see an empty/`no-data` output → catch the hallucinated-success bug). Build WITH the tool-card-train in the chat-overhaul batch. [[feedback_tool_card_expand_output]]

## DEMO-BLOCKING (user gate 2026-06-16)
NATE will run no further demos until the **duplicate-layer + LLM recompute** fixes land. Therefore **J8 (F17 LLM history)** and **J3 (F22 layer-id keying / dedupe)** are PROMOTED ahead of J2. These two unblock demos:
- J8 stops the recompute (so no duplicate same-name layer is created).
- J3 keys every layer control by layer_id (so even if duplicates exist, sliders don't sync) + dedupes same-name layers.

## Sequencing (avoid cross-panel regressions — the panels share App.tsx / LayerPanel / Map.tsx)
**Stage 1 — layout shell (do first; everything else builds on it)**
- **J1 (web):** drag-to-resize chat (left border) + Layers panel (right border); remove the expansion toggle. (F10, F11)

**Stage 2 — viz/report subsystem (depends on J1)**
- **J2 (web):** new collapsible+scrollable **Visualizations/Reports section** under Layers; items named/timestamped/NEW-badged; selecting opens a full-screen popup overlay; **Pelicun report + charts persist here** instead of flashing in chat; retire in-chat viz. (F1–F6)

**Stage 3 — layers-panel content (after J2; same files → sequence, don't parallelize)**
- **J3 (web):** opacity slider value↔position fix (F8); layer-name readability (F7); layers list scrollable (F9); legend bundled-with-layer + hides-with-layer (F13).

**Stage 4 — isolated (parallel-safe; different files)**
- **J4 (web):** Sign out → Settings. (F12) — DONE (committed)
- **J5 (web):** reset AOI/bbox overlay on Case exit (client replace-not-reconcile). (F14) — DONE (committed)
- **J6 (agent):** strip emojis from narration (adapter SYSTEM_PROMPT + output guard). (F15) — DONE (committed)

**Round-2 additions (from live testing 2026-06-16, after J1 started)**
- **J7 (web, F16):** payload-warning banner "hat" pinned above the chat window. Touches the chat-area shell → sequence right after J1 (shares the chat container + the lifted chatWidth). Rides on J1's chatWidth single-source-of-truth so the banner aligns to the chat column.
- **J8 (agent, F17):** rehydrate per-Case LLM history on case-open so follow-ups see prior work (no recompute). Agent-harness behavior change → gate with the 4-lens adversarial-verify panel (high-importance, routing-sensitive — job-0245 territory). Parallel-safe with web jobs. SRS-amendment-worthy (FR-AS context/continuity).
- **J9 (web, F18):** new tool cards append after the most recent prompt (fix the behind-last-prompt ordering). Chat stream/interleave (Chat.tsx) → sequence with the J1/J7 chat work to avoid churn on the same file.
- **F19** folds into **J2**: graphs/reports section lives in the BOTTOM HALF of the LEFT panel (below layers); report popup opens CENTERED; re-accessible. (Updates J2's spec — was "under Layers"; now explicitly bottom-half + centered popup.)

**Round-3 additions (from live testing 2026-06-16, mobile pass — TiTiler render fixed job-0314)**
- **F29 (web, mobile):** mobile Settings reachability + API-key bundling. (a) Move the Settings entry into the TOP-RIGHT on mobile so it is reachable from anywhere, not only the Cases panel. (b) Bundle the API-key fields into Settings (co-locate on mobile). Desktop Settings unaffected unless trivially shared. Bundle with the next web batch. Roads + colored relief confirmed rendering on mobile after job-0314.
- **F30 (agent/data) -> job-0316:** rivers reliability + FULL-BBOX coverage. LIVE DIAG 2026-06-16 (Chattanooga): `fetch_river_geometry` (NHDPlus HR) fails on HUC4 routing for most CONUS bboxes (OQ-39); `fetch_roads_osm` rejects `waterway` (highway-only); `fetch_nhdplus_nldi_navigate` (NLDI seed-point trace) WORKS and inline-renders (413 flowlines) BUT only draws the network hydrologically CONNECTED to the seed within distance_km — so it cuts off inside the bbox (unconnected sub-watersheds / downstream-of-seed / beyond range are missing). Root issue: "show rivers in this AREA" needs a BBOX/area query, NOT a network trace. Fix: a bbox rivers path that fills the whole AOI like roads — primary = OSM `waterway` Overpass bbox query (global, no HUC4 gap, same inline path as roads); then NHDPlus HR (authoritative US, when HUC4 resolves); NLDI trace reserved for point-to-network navigation (e.g. the seepage demo's downstream trace), NOT area fill. Wrap as one reliable `fetch_river_geometry` with internal fallback OSM-waterway -> NHDPlus-HR -> honest error (data-source-fallback norm). Rivers render inline (no publish). Pairs with MODFLOW river-seepage demo.
- **F41 (agent+web+schema, forward-looking):** user-gated spill/source LOCATION confirmation. When a spill location is ambiguous or low-confidence — vague news grab ("near the river"), uncertain geocode, or no explicit coords — GATE on the user before running the expensive MODFLOW model: show a confirm card that lets the user drop/drag an exact PIN on the map (or accept the extracted/geocoded point), then proceed. Explicit coords / clear place -> run directly (no gate). Mechanism EXISTS but is broken: the solver-confirm gate already tries to fire for run_model_groundwater_contamination_scenario (live log: "solver-confirm gate could not build the confirm card ... falling through") — fix + extend it to carry a location-pin. Reuse the payload-confirmation seam (sendPayloadConfirmation) + the interactive-map-input pattern from the PayloadWarning UX redesign (draggable bbox -> draggable POINT pin for point sources). Generalizes: ambiguous-input solver runs gate with interactive map input (pin for point sources, bbox for areas) + key params before spending the solve. Pairs with [[project_modflow_river_seepage_demo]] + [[feedback_payload_warning_ux_redesign]].
- **F44 (web, MOBILE — user-clarified 2026-06-16):** the MOBILE chat sheet should be drag-resizable (drag the handle to resize sheet height) AND tap-foldable (the tap-to-fold it already has). Today mobile only tap-folds to a fixed 70vh; add draggable height while keeping tap-fold. Desktop already has drag + collapse. Web batch.
- **F45 (web, MOBILE — user-clarified 2026-06-16):** on the MOBILE chat, move the model + connection-status signifiers HORIZONTALLY onto the top tab/header row (lay the model name + connected indicator out along the header aligned with the tab) instead of wherever they sit now. Small mobile header-layout. Web batch.
- **F42 (web/mobile):** the collapsed-chat mobile tool-usage strip (SheetActiveToolStrip) must show the SAME rainbow-gradient running animation as the inline pipeline cards (per [[feedback_pipeline_card_visual_states]]). Currently it's a plain label+timer. Web batch.
- **F43 (web) = re-confirm/escalate F13:** the color GRADIENT/legend bar must be ATTACHED to the bounding box (bottom edge of the AOI), NOT floating at the bottom of the screen — on BOTH desktop AND mobile. User re-raised 2026-06-16. This is the F13 legend-with-layer placement; ensure the attach-to-AOI anchoring works on desktop and mobile. Web batch (J3b/F13).
- **F29 PRIORITY-ESCALATED (user 2026-06-16):** mobile Settings is "very much an eyesore, in the way, and hard to get to" — move it to the top-right + bundle API keys inside. Bump to FRONT of the web batch.
- **F53 (web+agent) -> job-0325 (plumbing) + job-0325b mobile-gesture refinement:** DELETE layers + propagate deletion to the agent's view. PLUMBING (job-0325): WS delete command -> server removes the layer from session-state loaded_layers + persists (DynamoDB) + emits updated session-state. The map auto-removes it via replace-not-reconcile (no Map.tsx change needed). CRITICAL: the agent's loaded-layers awareness (build_layers_present_note / loaded_layers) must reflect deletions so the agent no longer thinks a deleted layer is present. UX (refinement, user 2026-06-16): DESKTOP = a delete control/button. MOBILE = a GESTURE so it does NOT clutter the (liked) mobile panel look: RECOMMENDED swipe-RIGHT-to-delete (horizontal swipe is distinct from the VERTICAL drag-to-reorder, so no conflict; iOS-familiar) -> a confirm "Delete this layer?" popup before it deletes. (Alt the user floated: long-press-without-movement, but that's ambiguous vs the reorder long-press.) Confirm popup on BOTH desktop + mobile. The mobile gesture refinement applies to LayerPanel.tsx AFTER job-0325's delete plumbing lands (same file -> sequential), folds into job-0322 / the web pass.
- **F54 (agent) -> job-0325:** do NOT re-fetch/re-compute a layer that is ALREADY loaded. User saw a wasteful re-pull when the layers to blend were already present. Strengthen build_layers_present_note (adapter.py) to include each loaded layer's HANDLE/uri so the agent can pass it directly (e.g. to compute_blended_composite) + a firm "reuse the existing handle, do NOT re-fetch/recompute unless it is absent" instruction. Relates to [[feedback_layer_visibility_contract]] + the F17 layers-present note.
- **F55 (web, BUG) -> job-0325:** mobile layer-visibility "eye" RESETS to visible when the Layers panel collapses. Visibility state must persist across collapse on mobile (verify desktop too). LayerPanel.tsx.
- **F56 (web) -> job-0322/next web pass:** chat-window background opacity. Current chat bg is too transparent -> cluttered. Add a CHAT OPACITY control in Settings (SettingsPopup), PERSIST PER-USER (not per-case), default MEDIUM (lightly frosted, MORE opaque than current ~0.96/0.58 alphas), apply to both desktop (desktopChatContainerStyle) + mobile (mobileSheetContainerStyle). Conflicts with job-0321 (SettingsPopup F29) + job-0322 (Chat.tsx) -> fold into job-0322 / the web pass after job-0321.
- **F52 (web, BUG — user 2026-06-16):** Cases panel tap-to-dismiss. Tapping empty space outside the Cases components does NOTHING — there's an invisible/non-functional backdrop. Make tapping ANYWHERE that is NOT a cases component dismiss/close the Cases panel (a real scrim/backdrop click-to-close). Likely CasesPanel.tsx / MobileDrawer.tsx (the drawer backdrop onClick -> close). -> job-0322.
- **F44/F45 ESCALATED (user re-requested 2026-06-16):** un-deferred — the grabbable MOBILE chat-window edge (F44 drag-resize sheet height + keep tap-fold) and the moved connection signifier + "GRACE-2"/version label on the mobile chat header (F45) ARE wanted now. They touch Chat.tsx — the SAME file as the F42 rainbow strip already running in job-0321 — so they CANNOT run concurrently; they go in **job-0322** (immediate follow-on, built on top of job-0321's Chat.tsx changes). job-0322 = F44 + F45 + F52, BUNDLED into the same WEB deploy as job-0321 (since job-0321 has not deployed yet — one web deploy, one header-hash bump, user tests everything at once).
- **ROUND-7 refinements (user 2026-06-16, post web-deploy 77c1bcc) -> job-0328:**
  - **F44 follow-up:** mobile chat sheet "still not draggable" per user. job-0322 claimed it; either broken/incomplete in the committed Chat.tsx or user is on the stale bundle (disambiguate via header hash). job-0328 must VERIFY the mobile sheet drag-resize actually works and fix it if not.
  - **F45 refine:** on the mobile chat drag tab/handle, lay out "GRACE-2 + version hash" on the LEFT and the connection status on the RIGHT (specific arrangement).
  - **F53 refine (user reversed the gesture call):** DROP the mobile swipe-to-delete; the delete ICON button is the preferred affordance on mobile too. The confirm dialog must NOT be an in-panel dialog — it must be a FULL-SCREEN popup overlay over the entire screen (like the other full-screen popups). LayerPanel.tsx + ConfirmationDialog (full-screen variant).
  Files: Chat.tsx (F44/F45) + LayerPanel.tsx/ConfirmationDialog (F53). Disjoint from job-0327 (App.tsx/BottomRowButtons settings). Bundles into the next web deploy with job-0327.
- **ROUND-8 (user 2026-06-16, post-77c1bcc live; job-0327 + job-0328 BOTH died on 529 API overload — nothing landed; consolidating ALL into ONE all-Opus multi-phase web workflow). Model directive: "use Opus for everything" — every build/verify agent = Opus 4.8 (see [[feedback-cost-discipline-model-routing]] override). Stale-vs-broken disambiguation DONE via orchestrator-direct code read: F52 + F31 are REAL incomplete fixes, NOT stale bundle.**
  - **F52 RE-DIAGNOSED (still broken):** tap-anywhere-but-a-component does NOT dismiss; the whole invisible left-panel column from top to bottom swallows taps so you can't tap the map to close. ROOT CAUSE: job-0322's `e.target === e.currentTarget` guard on the drawer column (MobileDrawer.tsx:146-147) only matches a tap on the BARE column element; the `flex:1` wrapper (App.tsx:1134) + the CasesPanel/LayerPanel card subtrees set `e.target` to a descendant, so those taps never close. FIX: `pointer-events:none` on the drawer column + its layout wrappers, `pointer-events:auto` ONLY on the actual interactive cards (CasesPanel root, CaseView, LayerPanel root, footer) → every non-component tap (incl. the full left gutter) falls through to the z=40 full-screen backdrop (onClick=onClose). Files: MobileDrawer.tsx + App.tsx (wrapper styles).
  - **F31 RE-DIAGNOSED (still broken on mobile):** exit browser + re-enter on mobile = NO layers until Case reopen. ROOT CAUSE: iOS WebSocket ZOMBIE — after backgrounding, `socket.readyState` stays OPEN but the connection is dead. job-0322's resume handler (App.tsx:649-667) calls `reconnect()` (no-ops on OPEN, ws.ts:497) then `requestSessionState()` (no-ops unless OPEN, else sends `session-resume` into the DEAD socket, ws.ts:468) → server never receives → no session-state echo → no repaint. FIX: add `forceReconnect()` to ws.ts (unconditional close+reopen regardless of readyState; fresh open re-sends auth-token + session-resume) and call it on `visibilitychange→visible` (at minimum on mobile). Files: ws.ts + App.tsx.
  - **F57 (NEW):** cases per-row actions are "three buttons in a line" (rename ✎ / archive ⟲ / delete ✕, CasesPanel.tsx CaseRow:207-243). User PICKED: replace with a single KEBAB (⋮) overflow button per row → popover menu Rename / Archive / Delete; Delete still routes through the full-screen ConfirmationDialog. Applies to desktop left-rail AND mobile drawer. File: CasesPanel.tsx.
  - **F58 (NEW):** the "+ New Case" button (CasesPanel.tsx:385) should be JUST A PLUS (icon, no "New Case" text). Folds into the icon pass / CasesPanel group. File: CasesPanel.tsx.
  - **F59 (NEW, PROJECT-WIDE UI POLICY):** NO EMOJIS / raw unicode glyphs in the UI, EVER — adopt a real ICON PACK. User wants an Apple/SF feel; SF Symbols are Apple-platform-license-only (CANNOT ship on web), so use a web pack with the SF aesthetic (recommend Phosphor — multi-weight, rounded-geometric; alts Lucide / Heroicons). PICK PENDING (AskUserQuestion). Then: (Phase 0) add the pack dependency + a shared icon module; (Phase 1) each behavioral group swaps its own file's glyphs (✎ ⟲ ✕ ⋮ ☰ ⚙ ▭ ⟳ ▾ ✓ etc.) to icon components; (Phase 2) a DEDICATED separate Opus agent does a repo-wide FINAL UI pass to replace/remove EVERY remaining glyph across all web files + enforce one consistent set + assert zero raw glyphs remain (user: "Have a separate opus agent do a final UI pass to replace or remove emojis with some icons"). Distinct from the existing no-emoji-NARRATION policy (J6, agent-side) — this is no-emoji-in-the-WEB-UI. Update SRS (Appendix I / FR-WC) to record the UI-icon-pack + no-glyph policy (propose; NATE lands).
  - **CONSOLIDATED DISPATCH PLAN (job-0329, all-Opus, gated on F59 pack pick):** Phase 0 icon-pack foundation (1 Opus, serial) → Phase 1 behavioral+glyph groups (parallel, file-disjoint): A shell {App.tsx, ws.ts, MobileDrawer.tsx} = F29(drawer-footer settings removal)+F31(forceReconnect)+F52(pointer-events); B chat {Chat.tsx} = F44(drag)+F45(header layout); C layers {LayerPanel.tsx, ConfirmationDialog.tsx} = F53(drop swipe + portal confirm to body); D cases {CasesPanel.tsx} = F57(kebab)+F58(add-case plus) → Phase 2 final emoji→icon sweep (1 dedicated Opus, serial) → Phase 3 verify (Opus, refute-by-default, BEHAVIOR-focused not just "handler exists", gate). Then commit passing groups → ONE web deploy (vite + s3 + CloudFront), user-authorized. Replaces the dead job-0327 + job-0328.
- **F60 (NEW, web+agent+schema — user 2026-06-16) -> job-0330 (QUEUED after job-0329; file overlap Chat.tsx/ws.ts + reuses 0329 icon module):** chat composer redesign + Claude-Code-style tool-permission MODES. WEB: move the text form up a bit; left of Send add paperclip (file-drop) + mic icons (both STUBS for v0.1) in order paperclip, mic, Send; FAR-LEFT mode chip -> taps open a sheet that SLIDES UP FROM BOTTOM to pick Normal (default) / Strict (gates EVERY tool use — agent pauses + asks per tool) / Auto (auto-yes to all tools, EXCEPT existing size/time safety gates still prompt). AGENT: Strict = generalize the payload-warning pause/resume to a per-tool approval gate (tool-approval-request/response); Auto = skip per-tool gate, keep size/time gates; Normal = today. SCHEMA: approval envelopes + mode on user-message. Reuses [[feedback_large_payload_chat_warning]] pause pattern + [[feedback_tool_retry_on_failure]] for denials. Propose SRS Appendix D/F amendment (NATE lands). Confirm mode persistence scope (default per-case) before final wiring.
- **ROUND-9 (user 2026-06-16, mobile chat-handle refinements -> job-0332; SEQUENTIAL after job-0329 Group B commits Chat.tsx — same file). job-0329's F44 (actually-draggable) stays; job-0332 refines the handle row + safe-area on top.**
  - **F45 REFINED (supersedes prior F45):** the chat-sheet HANDLE is ONE line with THREE zones: LEFT = (GRACE-2 + version hash), CENTER = the grabber rectangle (drag handle), RIGHT = (connection status). Layout: (grace + version) < grabber rectangle > (connection status).
  - **F45b (collapsed vs expanded):** the labeled row (grace/version + connection) appears ONLY when the chat is EXPANDED. COLLAPSED = just the plain grabber rectangle snapped to the TOP of the chat box, no labels; when tools ARE in use, the rainbow active-tool strip (F42) fills the area in between (the middle) instead of the labels.
  - **F61 (NEW):** bump the mobile chat sheet UP a bit more so it clears the iPhone's naturally curved corners / home-indicator — increase bottom safe-area clearance (env(safe-area-inset-bottom) + extra). Extends the earlier "move the text form up a tad" (F60 composer). Chat.tsx.
  - **F66 (web, collapsed-mobile sandbox strip) -> job-0332:** when the chat is COLLAPSED on mobile, the Python-sandbox (code_exec) card shows in the active-tool strip the SAME WAY tool usage does (F42/F45b SheetActiveToolStrip), but with a PULSATING BLUE animation instead of the rainbow gradient; multiple active strips STACK. Respect prefers-reduced-motion. Chat.tsx.
  Files: Chat.tsx. Bundles into the next web deploy with job-0329 (or its own follow-on deploy).
- **ROUND-10 (user 2026-06-16, Lincoln NE sandbox + redundant-recompute + gating UX):**
  - **Sandbox wiring CONFIRMED (not a bug):** code_exec_request (Python sandbox) IS wired on AWS — GRACE2_STORAGE_BACKEND=s3 -> sandbox_runner._is_local_mode()=True -> runs LOCALLY on EC2 (synchronous subprocess, 60s hard cap), NOT the dead GCP Cloud Run path. A CONFIRMED run returns within ~60s. The "stuck/never-returns" run = orphaned APPROVAL GATE (code_exec_request is fail-closed: confirmed=False raises CodeExecConfirmationRequired; user must approve the card; user sent another prompt before approving -> orphaned). Fix = F63.
  - **F62 (agent, recompute) -> job-0333 (RUNNING, all-Opus):** agent redundantly RE-RUNS expensive sims (SFINCS/MODFLOW/Pelicun) + re-fetches/re-computes layers ALREADY present. Extends F54 (soft steer ignored). Fix: HARD SYSTEM_PROMPT reuse rule + richer build_layers_present_note (per-layer identity: name/type/role/scenario/bbox/handle) + DETERMINISTIC reuse guard in run_model_* path (match scenario+bbox+params -> return existing handle, don't re-launch). Agent restart (gated) bundles with job-0331 footprints.
  - **F63 (web+agent, gated-tool double-prompt) -> job-0330:** sending a new prompt while a tool approval (sandbox/payload/sim) is PENDING orphans the gate. Handle gracefully: keep the gate live/resumable OR cancel cleanly with a clear message (never silent limbo). Server.py confirmation flow + web.
  - **F64 (web, unify gating UX) -> job-0330:** large-data + sim warnings should be INLINE CHAT CARDS that BLOCK prompting (same pattern as the sandbox approval card), NOT the current floating OVERLAY above chat. Unify all user-gated interruptions (sandbox approve, payload warning, sim warning) as inline blocking chat cards. Supersedes/extends [[feedback_payload_warning_ux_redesign]] (the overlay redesign) -> now it's an inline card. Chat.tsx + the warning/approval card components.
  - **F65 (web, stop button) -> job-0330:** a STOP button that REPLACES the Send button while the agent / a tool is executing (Claude-Code style) -> wired to the existing ws.ts sendCancel. ChatInput.tsx / Chat.tsx.
  - job-0330 now = composer redesign (paperclip/mic/send stubs + form-up) + Normal/Strict/Auto modes (slide-up sheet) + F63 (double-prompt gate) + F64 (inline blocking gating cards) + F65 (stop button). Cohesive chat-composer + tool-gating overhaul (web ChatInput/Chat + agent server.py gating + schema approval envelopes). Sequential after job-0332 (both touch Chat.tsx).
- **F67 (web, tool-card foldable output) -> job-0334 (un-deferred 2026-06-17, user "queue it up next"):** every tool usage card (incl. the Python sandbox card) gets an expand chevron revealing TWO labeled sections — "Command:" (the tool call / args; for the sandbox, the python code) and "Output:" (the raw function_response; for the sandbox, stdout + result). Claude-Code-style collapsible; success (green) + failure (red) cards; truncate large payloads w/ popup escape hatch. Best live defense vs no-data/hallucinated-success. See [[feedback_tool_card_expand_output]]. DESIGN (user mockup 2026-06-17 IMG_1098 = Claude Code's mobile tool-output sheet): clicking a tool card -> MOBILE = a FULL-SCREEN PULLOVER sheet (X top-left, tool name centered header, "Command" + "Output" monospace sections, scrollable); DESKTOP = expand the card DOWNWARDS inline, show ~15 lines of Output then a "Show more" button, all inline in the chat scroll. CROSS-LAYER (raw payload NOT on the wire today - verified PipelineCard gets only humanized label+status; only the sandbox carries stdout_tail): SCHEMA add raw command (tool+args / sandbox python_code) + raw output (function_response, bounded/truncated like sandbox stdout_tail) to the tool-card/pipeline-state envelope; AGENT populate (pipeline_emitter.py / server.py); WEB clickable card + mobile pullover component + desktop inline-expand (PipelineCard / InlineChatCard / SandboxCard + Chat.tsx). Overlaps server.py (job-0333) + Chat.tsx (job-0332) -> SEQUENCE after both commit. Multi-group workflow (schema -> agent-emit -> web-render).
- **ROUND-11 (user 2026-06-17, live-testing the 2e3dfd9 deploy):**
  - **F52 CONFIRMED WORKING** (tap-anywhere-to-collapse). 
  - **F68 (web, BUG) -> job-0335 (RUNNING):** mobile Cases rows CUT OFF horizontally at the kebab (3-dots); only fully visible in LANDSCAPE. Likely CasesPanel fixed width:260 (desktop rail width) not adapting to the min(320,85vw) drawer column in portrait + the kebab pushing past the column overflow:hidden clip. Fix: full-width cases panel on mobile, title flex:1+min-width:0 (ellipsis), kebab flex-shrink:0 always visible, menu popover not clipped. Files CasesPanel.tsx + App.tsx + MobileDrawer.tsx (disjoint from job-0332 Chat.tsx). User hypothesis: an invisible panel (settings button container?) overlays + clips the cases panel — agent to check.
  - **F56 RE-BROKEN (web, BUG):** chat opacity control "still doesn't work." Verified WIRED (SettingsPopup control + Chat.tsx chatOpacityAlphas/LS_CHAT_OPACITY, job-0322) but NOT reactive — changing the tier in Settings does not re-apply to the chat container live (likely Chat doesn't subscribe to the localStorage tier change / no re-render on change). Re-fix: make Chat reactively read + apply the opacity tier on change (storage event or shared state). Touches Chat.tsx -> SEQUENCE after job-0332 commits. Bundle with the next Chat.tsx job (0334 web group or its own quick job).
  - **Already-queued confirmations (told user):** grace+version-only-when-expanded / collapsed=plain-grabber+tools-in-space = F45b job-0332 (running, not deployed); paperclip/mic/send stubs + text-form move-up = F60 job-0330 (queued); F61 safe-area = job-0332.
- **ROUND-12 (user 2026-06-17):**
  - **F69 (agent, BUG) -> job-0336 (RUNNING):** "tornado touchdowns in Oklahoma since 2000" failed — fetch_storm_events_db._validate hard-rejects non-2-char states ("Oklahoma" -> StormEventsArgError) though the query already tolerates full names; AND the circuit breaker trips on that CLIENT/arg error (counts ANY exception), so 3 bad-arg parallel-year calls tripped it + the 60s cooldown blocked the corrected retry; agent punted "wait 60s". Fix: accept full state names + breaker trips only on UPSTREAM/transient (not client/arg) errors + adapter self-correct-and-retry steer (don't punt). circuit_breaker.py + server.py + fetch_storm_events_db.py + adapter.py.
  - **F70 (web, polish):** the glowing-rainbow RUNNING tool-card animation was SKIPPED on fast-fail — cards "went straight to failing." For instant-fail tools the running/rainbow state never showed. Ensure the running treatment renders even briefly (min running-state dwell) OR at least the card reflects attempted->failed clearly. Relates to [[feedback_pipeline_card_visual_states]] + [[feedback_pipeline_card_failure_terminates_animation]]. Web PipelineCard. Bundle with job-0334 card work.
  - **F71 (agent, geocoder quality):** geocode_location resolves REGIONAL/vernacular queries poorly — "South Florida" -> a minor Kansas locality, then a narrow point. The agent self-corrected to a known South-FL bbox (good degrade), but improve: recognize region-level / multi-county queries (e.g. "South Florida", metro names) and resolve to a sensible region bbox (Census place/CBSA/county-group lookup) or steer the agent to a region bbox for vernacular regions. Not a blocker (self-corrected). Own job later.
  - **F72 (web, RESIZABLE AOI bbox):** user re-raised — a draggable/resizable AOI bounding box so the user can adjust scope (esp. when geocode/agent picks a tiny bbox). Relates to [[feedback_payload_warning_ux_redesign]] (interactive bbox editing for narrow-scope warnings). Web (Map.tsx draggable rectangle handles + AOI state). Own job.
  - **F73 (agent/web, AUTO-FIT bbox to data):** "ask it to resize the box to encompass all protected areas" — after a fetch, be able to re-fit the AOI / zoom to the EXTENT of the fetched features (auto-fit-to-data). Either an agent action (recompute AOI = union of feature bounds) or a tool/map action. Nice touch; pairs with F72. Own job. (User testing this now.)
  - **F73 SHARPENED (agent/engine):** user tried "resize the bbox" via chat -> the agent kicked off a PYTHON SANDBOX run (stuck/orphaned again) to compute the extent. WRONG TOOL. Build a DEDICATED atomic tool: compute a vector layer's bounds (min/max lon/lat over the features -> bbox), so "resize the box to encompass all <features>" / auto-fit AOI is a fast deterministic tool call, NOT the gated sandbox. New tool (e.g. compute_layer_bounds / fit_aoi_to_layer) + catalog entry + adapter steer ("to fit the AOI to a layer's extent, use <tool>, NEVER the sandbox"). Agent/engine. Sequence after job-0336 (adapter.py). Pairs with F72 (resizable bbox) + F73 (auto-fit).
  - **F74 (web FEATURE + agent honesty) — KEY FINDING:** the agent narrates "click individual polygons to see name / designation / IUCN" but there is NO feature-click/popup handler ANYWHERE in web/src (grep: zero queryRenderedFeatures / Popup / click handlers). The capability is HALLUCINATED — not a mobile bug, it was never built (not mobile, not desktop). TWO fixes: (a) WEB FEATURE — build tap/click-to-inspect: query the vector layer at the tap point -> popup with the feature's attributes (name, designation, IUCN, etc.); MUST work on mobile (touch) + desktop. Map.tsx + a popup component. (b) AGENT HONESTY — adapter steer: do NOT tell users to "click polygons for data" unless the inspect feature is actually supported (Invariant: never claim unbuilt UX). Relates to [[feedback_synthetic_close_out_design]] honesty. (a)=web own job; (b)=adapter steer (sequence after job-0336).
  - **Stuck sandbox (AGAIN):** the resize-via-chat sandbox is orphaned/can't-stop -> reconfirms F63 (gated-tool double-prompt) + F65 (stop button) urgency in job-0330.
  - **F73 SHARPENED AGAIN (the "random house"):** user asked again to resize the box; agent computed total_bounds (sandbox) but the AOI stayed a TINY box "around some random house" = the original geocode bbox was NEVER updated (the agent CANNOT resize the AOI — it even said "I can't pan/zoom", though zoom-to via fitBounds DOES exist). So F73 must include the ACTUAL capability: (1) compute_layer_bounds tool (total_bounds; reuse the postprocess_pelicun helper), (2) a map-command to RESIZE the AOI rectangle (analysis extent) to given bounds AND/OR zoom-to-fit the view (zoom-to map-command already exists in Map.tsx fitBounds — the agent must USE it), (3) adapter steer: to fit/resize the AOI to a layer use these, NEVER the sandbox, and you CAN drive the map (stop claiming you can't). Agent (tool+server map-command+adapter) + maybe web (AOI-resize map-command handler). After job-0336.
  - **F75 / job-0338 (SANDBOX UX — PULLED FORWARD from job-0330 due to repeated user pain):** the Python sandbox is opaque + unstoppable: no STOP affordance (F65), no clear RUNNING/DONE state on the sandbox card (user "can't tell if it's running or completed"), and the agent loses track of its own pending run (F63 orphan on double-prompt). job-0338 = (a) STOP button replacing Send while a tool/sandbox runs (wired to ws.ts sendCancel); (b) sandbox card shows a clear RUNNING spinner -> DONE/FAILED terminal state (and surfaces the result); (c) gated-tool double-prompt handling (don't orphan; keep the gate live/resumable or cancel cleanly with a message). Touches Chat.tsx + SandboxCard + server.py (cancel/gate). Sequence AFTER job-0337 (Chat.tsx) + job-0336 (server.py) commit. Subset of job-0330's gating scope, landed sooner. The full Normal/Strict/Auto modes + paperclip/mic stubs + inline-card gating (F60/F64) remain in job-0330.
  - **F76 (STRETCH, post-v0.1) — self-made/learned tool repository (NATE 2026-06-17):** promote SUCCESSFUL agent sandbox snippets into reusable saved tools; case-made tools -> user-global tools (defer cross-user shareable for a security review). Self-extending toolbox that grows with usage. Promotion = GENERALIZE (name + LLM description + typed params), NOT save-verbatim; same hardened sandbox; persist via MongoDB MCP; join the catalog; "My Tools" curation UI. Build AFTER job-0338 (sandbox UX) + F73 (dedicated tools/AOI) + job-0334 (Command/Output fold) are solid. See [[project_self_made_tools_repo]]. Orchestrator assessment: strong idea (skill-library learning loop). SRS amendment when it leaves stretch.
- **ROUND-13 (user 2026-06-17):**
  - **Icon migration LIVE-VALIDATED:** user on 2e3dfd9 confirms the Phosphor UI icons "look good" (job-0329 landed cleanly on-device).
  - **Routing-quality location (answered, not a bug):** RoutingQualityDashboard is reached via Settings(gear) -> "Tools" section -> "Routing quality" (SettingsPopup onOpenRoutingDashboard; App.tsx:1447 wires it, :1469 mounts) — NOT the tool-catalog popup. Wiring intact post-migration. User looked in the catalog popup.
  - **F77 (web FEATURE — routing-quality as a READINESS MAP, user endorsed):** surface routing quality IN the tool catalog (where the user looked) + per-tool READINESS indicators so the user can see which tools are proven/good-to-go (chaining or single-use) vs which still need demoing/live-testing. Builds on the existing RoutingQualityDashboard telemetry. PREREQ: verify the routing telemetry is actually populated on AWS (DynamoDB) before building (else empty). Web (ToolsCatalogPopup + RoutingQualityDashboard) — disjoint from job-0337/0339. A near-term web job once telemetry confirmed.
  - **STALE TEST (cleanup, NOT a regression):** tests/test_qgis_discovery.py::test_qgis_process_pass_through_invokes_bound_submitter fails ("DID NOT RAISE NotImplementedError") — stale assertion left by committed f0f716a (job-0308) which changed qgis_process to no longer raise. Independent of recent jobs. One-line test update; fold into the next agent batch.
- **F78 / job-0340 (SECRETS / CREDENTIAL PIPELINE — user 2026-06-17, "that's what this was for") — HIGH IMPORTANCE (auth boundary -> adversarial-verify per [[feedback_adversarial_verify_high_importance]]):** the user's intended flow on a key-required data source: tool auth-fails -> agent surfaces a CREDENTIAL-REQUEST card (provider name + SIGNUP LINK + key-entry form) -> user gets the key in another tab + enters it -> saved to the user's vault + shows in Settings -> API keys (SecretsPanel) -> agent RETRIES with the key piped DIRECTLY into the request (never to the LLM). LIVE TEST ("active fire hotspots in California") failed: FIRMS_AUTH_ERROR -> agent just narrated a workaround (visit FIRMS / use NIFC perimeters); no key-request, no retry.\n  DIAGNOSIS: (1) fetch_firms_active_fire._resolve_map_key reads ENV ONLY (os.environ GRACE2_FIRMS_MAP_KEY, fallback "demo") — never the vault. The CORRECT pattern already exists in fetch_ebird_observations._resolve_api_key (explicit kwarg -> secret_ref -> Persistence.get_secret_value() from the vault; cache key excludes the key) — generalize it. (2) No credential-request envelope/flow: an auth error doesn't pause+ask for a key.\n  SCOPE (multi-group, cross-layer): (A) AGENT/ENGINE — generalize the eBird secret_ref->vault resolution to FIRMS + every env-only key tool (era5/movebank/gtsm/iucn/firms...); thread the per-user secret_ref to the tool at call time; (B) AGENT — classify missing/invalid-credential errors (FIRMS_AUTH_ERROR class) -> PAUSE + emit a credential-request envelope (provider id + signup URL + secret key name) -> on credential-provided, RETRY the tool (now resolves the key); reuse the payload-warning pause pattern. (C) SCHEMA — credential-request / credential-provided envelopes (or reuse secret-add). (D) WEB — inline credential-request card in chat (signup link + key entry form) that routes to secret-add (vault) + signals retry; key appears in Settings->API keys (SecretsPanel — confirm secret-add -> secrets-list listing works). (E) per-provider METADATA — signup URL + provider id + secret key name (catalog already has credential_tier + api_key_secret_ref; add signup URLs). Overlaps server.py + adapter.py (agent) + Chat.tsx + SettingsPopup/SecretsPanel (web) -> coordinate sequencing with job-0338/0330 (gating) + the pending web/agent deploys. Build as a verified multi-group workflow; FIRMS as the end-to-end proof.
- **F79 (agent, rate-limit hints — user 2026-06-17):** add a SMALL per-endpoint rate-limit note to each external-API tool's DESCRIPTION (docstring = the LLM-facing surface) so the agent paces itself + doesn't hammer (e.g. storm-events "one year at a time, avoid firing many years concurrently"; FIRMS / per-key endpoints note their limits). Layered defense: rate-note (PREVENT) -> [optional v2] a structured rate_limit metadata field + a real per-endpoint concurrency/throttle in dispatch (ENFORCE) -> circuit breaker job-0336 (BACKSTOP). Plus a general adapter steer: "respect each tool's rate-limit note; do not fan out many concurrent calls to the same endpoint; sequence/batch." v1 = docstring notes on the worst offenders (storm-events, FIRMS, fetch_* external APIs). Agent-side (tool docstrings + AtomicToolMetadata optional field + adapter). Fold the credential-tool rate-notes into job-0340; broader pass = small own job. Complements [[feedback_data_source_fallback_norm]].
- **F80 (agent infra, sliding-window rate tracker — user 2026-06-17):** a per-API SLIDING-WINDOW call counter so we know when we're approaching an endpoint's limit and can (a) THROTTLE/slow the next calls (backpressure) and (b) WARN the user honestly ("approaching the FIRMS rate limit — slowing down / results may be delayed"; never silent). Design: new rate_limiter.py (sibling of circuit_breaker.py) records each external call timestamp per API + a per-API LIMIT REGISTRY (known/conservative limits); checked before each external fetch in the dispatch path. WINDOW SCOPE FOLLOWS KEY SCOPE: shared deployment keys (FIRMS MAP_KEY, Overpass) -> GLOBAL window (limit shared across all users); per-user vault keys (job-0340) -> PER-USER window. Layered with F79 (soft note -> PREVENT), F80 (window -> ENFORCE/WARN), circuit breaker job-0336 (BACKSTOP). Emits a user-facing near-limit warning envelope (reuse the payload-warning/gating card family). Feeds the readiness map (F77 — call volume + limit-proximity per tool). Agent-side (server.py dispatch + rate_limiter.py + a warning envelope + web warning card). Sequence after job-0340 (overlaps server.py + the gating-card family). Substantial — own verified job.
- **ROUND-14 (user 2026-06-17) -> job-0342 (sequence AFTER job-0341 commits — shares CasesPanel.tsx + App.tsx):**
  - **F83 (web, cases density):** put the "x hr ago" timestamp INLINE with the case name (same row) to REDUCE each case row's height so more cases fit in the list. Move the updated_at from the meta row up next to the title (right-aligned). CasesPanel.tsx.
  - **F84 (web, BUG — cross-case layer persistence + fresh-slate):** layers from a previous case (e.g. WDPA protected areas — VECTOR / inline GeoJSON) STILL RENDER when switching to a new case / doing something else. ROOT-CAUSE HYPOTHESIS: the case-switch replace-not-reconcile (App.tsx case-switch effect pushes the new case's loaded_layers; Map.tsx reconciles) removes WMS RASTER layers but NOT inline-GeoJSON VECTOR layers (added later job-0175) -> vectors persist. DESIRED (user): going to CASES root (exit a case) CLEARS the UI to a FRESH SLATE — remove ALL map layers (raster AND vector) + the AOI bbox rectangle, nothing on the map; then when a case is generated / auto-generated, SHOW the new case's bounding box. FIX: Map.tsx reconcile must remove vector (geojson) sources/layers not in the new loaded_layers (not just WMS); App.tsx case-exit/switch pushes the empty/clean state + clears the AOI; new-case shows its bbox via the existing zoom-to/extent path. Files: Map.tsx (vector removal in reconcile) + App.tsx (case-switch/exit reset). Relates to [[feedback_wave48_known_bugs]] (replace-not-reconcile client-side) + ux-batch-1 F14 (reset AOI on case exit). Real cross-case contamination bug — priority within the next web batch.
- **ROUND-15 (user 2026-06-17, NWS-alerts + land-cover-ambiguous test):**
  - **F85 (FEATURE — interactive AOI selection via choropleth, user "does this make sense?" YES):** on an AMBIGUOUS-scope request (e.g. "land cover for South Florida", or a too-large AOI like all-of-Florida exceeding the NLCD 10k km² tile limit), instead of guessing a bbox or failing, render a SELECTABLE CHOROPLETH of candidate regions at the granularity that fits the use case (state -> county -> city/place -> neighborhood), and let the user TAP a cell to pick the AOI -> the tapped polygon's geometry becomes the case AOI -> the agent proceeds with the original request scoped to it. Solves the geocoder-ambiguity pain (F71, the South-FL->Kansas problem) intuitively. Reuses: F74b feature-click, fetch_administrative_boundaries (TIGER/Line counties/places), compute_layer_bounds/zoom-to (job-0339). Agent (detect ambiguity -> fetch candidate-region boundaries -> emit a "select a region" selectable-choropleth + prompt) + web (selectable choropleth + tap-to-set-AOI). Near-term feature; pairs with F72 (resizable bbox) as the two AOI-specification UXs. Own multi-group job.
  - **F86 (web, F74b refinement):** the feature-inspect popup renders in a FIXED spot — it should render AT the click/tap point (from the event lngLat/point) and PERSIST until the user taps a new feature or taps empty space (no choropleth) to dismiss. So you can zoom out and still read it where you clicked. FeaturePopup.tsx + Map.tsx (position from click coords). Overlaps Map.tsx (job-0342) -> sequence after job-0342.
  - **F87 (web, BUG — F31 STILL broken):** layers STILL disappear on browser background/resume despite job-0329's forceReconnect (deployed 2e3dfd9/534d2f2). RE-DIAGNOSE: hypothesis — the resume path (visibilitychange -> forceReconnect -> session-resume) restores the SESSION but NOT the active CASE's persisted layers; case-reopen restores them (they ARE persisted to the case) but resume does not re-open the case. FIX likely: on resume, re-open/re-hydrate the ACTIVE CASE (restore its persisted loaded_layers) rather than only session-resume; ensure Map repaints. App.tsx + ws.ts (+ maybe agent session/case-resume). Overlaps App.tsx (job-0342) -> sequence after. Recurring high-annoyance bug.
  - **F88 (verify, low-confidence):** NWS severe-weather-alert layer loaded but user saw NO polygons for Florida. Likely just no active FL alerts right now (per-state, can be empty) — verify empty-vs-render-bug (fetch_nws_alerts_conus / the alert-polygon render path). Note + check; not confirmed a bug.
- **F89 (NEW data source — Fields of The World / FTW, user 2026-06-17 "should be an added tool"):** access FOTW to render + pull agricultural FIELD-BOUNDARY vectors. Research (2026-06-17): TWO paths — (A) PUBLISHED VECTORS: fiboa / FTW benchmark field boundaries (GeoJSON/GeoParquet, no auth) on Source Cooperative source.coop/kerner-lab/fields-of-the-world + fiboa.org datasets; reliable, quick fetch, BUT coverage = ~24-country benchmark regions (NOT global). (B) ON-DEMAND INFERENCE: ftw-inference-api (github.com/fieldsoftheworld/ftw-inference-api) — AOI -> model -> field vectors from Sentinel-2; the "fields anywhere" UX, but engine-grade (public hosted endpoint "under active development", no documented URL/auth -> likely verify-their-API or SELF-HOST the inference model as a worker, SFINCS/MODFLOW-class). DESIGN FORK pending NATE: A=fetch_field_boundaries published-vector fetcher now (inline-GeoJSON render like roads/WDPA); B=on-demand-inference engine; C=both (A now, B as a follow-on engine job). New fetcher tool fits the conservation/ag pattern (fetch_buildings/fetch_roads_osm). Apply [[feedback_data_source_fallback_norm]] (A primary where coverage, B fallback / honest error). Sources: fieldsofthe.world, fiboa.org, source.coop/kerner-lab/fields-of-the-world.
  - **DECISION (NATE 2026-06-17): BOTH — fetcher now, inference next.** v1 = job-0343 `fetch_field_boundaries` published-vector fetcher (fiboa/FTW from Source Cooperative; inline-GeoJSON render like roads/WDPA; honest "no coverage" typed error where the benchmark lacks data) — fires RIGHT AFTER job-0340b commits (keep the agent tree clean for the FIRMS demo job; new tool touches tools/__init__.py + categories.py). v2 = follow-on ENGINE job: on-demand ftw-inference (verify ftw-inference-api public endpoint OR self-host the model as a worker; AOI -> Sentinel-2 -> field polygons). fetch_field_boundaries degrades published->inference->honest error per the fallback norm once v2 lands.
- **F40 (web):** AOI bounding box must be OUTLINE-ONLY, not a full-color fill. Current analysis-extent rectangle (Map.tsx drawExtent) has a solid fill that mixes with / tints the actual layer content inside the box. Fix: stroke-only (line layer / fill-opacity ~0 + visible border), so the box frames the AOI without coloring over the content. Small styling change. Web batch.
- **F31 (web/mobile) -> job-0322 (user re-requested 2026-06-16, "add to the tweaks you're working on now"):** mobile background/resume bug — leaving the browser and returning shows NO layers until the user exits to Cases and re-opens the Case. WS likely drops on mobile tab suspend; on `visibilitychange`/resume reconnect + re-request session-state and repaint layers (or keep map layers mounted across WS blips). Client lifecycle (Map.tsx/App.tsx/ws). Goes in job-0322 (NOT concurrent with job-0321 — its Map.tsx part conflicts with job-0321's F40/F43 Map.tsx work); bundles into the same web deploy. Relates to Wave 4.8 client-state-not-reset theme.
- **F34 (agent):** land cover (and ALL area layers) must use the FULL consistent case AOI bbox — do NOT shrink. LIVE 2026-06-16: agent fetched landcover at a smaller bbox (E=-85.187,N=35.127) than the case AOI / roads+rivers (E=-85.114,N=35.228), so it only covered the city. Triggered by the user's "you don't need to fetch all" being misread as "fetch a smaller area" (user meant "don't fetch every layer"). Fix = reuse the case AOI bbox for every area fetch (relates to F20 same-bbox-as-original-run); system-prompt clarity that "don't fetch all" never means shrink the AOI. Pairs with F33 (overviews).
- **F46 (engine+schema+web, FEATURE):** TIME-SERIES temporal layers + animation. User wants to watch a hazard evolve over time slots (a play/animate slider): contaminant plume moving downstream over time, AND flood depth over time. The engines ALREADY compute timeseries — we currently COLLAPSE to peak (postprocess_flood takes timemax per job-0058; postprocess_modflow takes the final UCN timestep). To animate: (engine) postprocess emits a per-timestep COG stack (or multi-band COG) + a time-index manifest instead of only the peak; (schema) a temporal/time-series LayerURI envelope carrying the timestep COGs + timestamps; (web) a time slider + play/pause that swaps the displayed timestep on the map. Applies to flood, MODFLOW-GWT concentration, and any future time-varying hazard. Pairs with the river-seepage demo ([[project_modflow_river_seepage_demo]] — "moving down the river over time" = this animation applied to GWT). Notable multi-part feature; own track.
- **F48 (agent, tech-debt):** "gemini" -> bedrock/llm naming cleanup. PROD inference is Bedrock Claude Sonnet (MODEL_PROVIDER=bedrock, BEDROCK_MODEL_ID=us.anthropic.claude-sonnet-4-6, systemd "GRACE-2 Bedrock agent"), confirmed. But the code keeps google-genai SDK as the tool-schema/content TYPE IR (bedrock_adapter translates to Converse at the boundary), and logs/identifiers still say "gemini" (e.g. "gemini function-call"), the verifier said "Gemini-declaration", and model_provider() DEFAULTS to vertex. Cleanup: rename gemini->bedrock/llm across log labels + identifiers, and PIN MODEL_PROVIDER=bedrock explicitly in deploy (the default-vertex fallback is load-bearing). No behavior change; reduce confusion. (User flagged 2026-06-16 — thought we might still be on Gemini.)
- **F49 (engine/QGIS, RESEARCH note):** MDAL + Crayfish + QGIS mesh pipeline for water modelers. From a "QGIS for water modelers" lecture: MDAL = mesh DATA I/O (the QGIS mesh-layer backend), QGIS = data interpolation / rendering / identify tool / styling / MESH CALCULATOR, Crayfish plugin = plots + ANIMATION export + export-to-vector. This is the natural pipeline for MESH-based, TIME-VARYING hydrodynamic output — directly relevant to TELEMAC (mesh engine), the time-series ANIMATION feature (F46), and the QGIS Processing substrate ([[project_qgis_processing_as_agentic_compute_substrate]] / sprint-16 job-0308). Well-documented (it's a standard QGIS-mesh workflow). Look into when algorithm/plugin discovery (job-0308) lands: wrap MDAL read + QGIS mesh-calculator/styling + Crayfish animation export as LLM tools so mesh engines render + animate the same way rasters do. Forward-looking.
- **job-0320 (agent/engine, QUEUED) — audit-fix batch:** from the 42-tool audit (wf wrab1b8b2). BROKEN: Tier-1 STAC path (`_tier1_stac_fetch` raises NotImplementedError; ESA WorldCover + MS Buildings STAC branches stubbed -> no catalog-discoverable global DEM/landcover STAC actually fetchable). NEEDS-FIX: catalog_search, catalog_fetch, fetch_mrms_qpe, fetch_nws_event, fetch_gcn250_curve_numbers, fetch_usfs_canopy_fuels, fetch_iucn_red_list_range. ~40 fetchers otherwise clean/minor. Fold into a fix batch (file-disjoint, refute-by-default), STAC stub = headline (ties to the user's STAC question — advertised in catalog but not wired).
- **F47 (engine, FUTURE):** HEC-HMS engine (hydrologic rainfall-runoff). Like TELEMAC, it is NOT built — only planned in solver.py comments ("TELEMAC / MODFLOW / HEC-HMS land later"). User wants it built AFTER one more SFINCS demo verification (the inland-flood fix job-0318). Build = deck builder + container/runner + run workflow + postprocess, like the SFINCS/MODFLOW engine builds. Sequence: SFINCS inland re-verify -> then HEC-HMS.
- **F50 (engine+web, demo enhancement):** model BUILDING FOOTPRINTS + color-code by Pelicun DAMAGE LEVEL. LIVE 2026-06-16 (Asheville): the building-inventory layer and the Pelicun damage overlay look IDENTICAL (same representation — NSI points, not footprints). User (re-raised): model actual building FOOTPRINT POLYGONS and color them by damage state (DS0..DS4). The data exists — the Pelicun output already carries per-structure damage states (screenshot: 71,987 structures, 2,961 DS1+, 0 DS4). Need: a footprint source (MS Building Footprints / OSM buildings / NSI-joined footprints) -> join per-building damage state -> render as colored polygons (graduated DS0-green..DS4-red). Pairs with [[project_pelicun_impact_postprocessor]]. High-value demo headline.
- **F51 (web, BUG):** Pelicun damage-state chart renders BLANK on mobile with "undefined is not an object (evaluating 'l.marktype')" (iOS Safari). The vega_lite_spec is VALID (bar mark, from chart_tools.py) and the summary line renders (71,987 structures...), so it is a vega-embed RENDER failure, not a bad spec. Stack: vega ^6.2.0 / vega-lite ^6.4.3 / vega-embed ^7.1.0, dynamic-imported into a ref'd div (ChartGallery + ChartStack). Likely cause: chart container has zero dimensions at embed time on mobile (popup not laid out yet) OR a vega-embed/Safari canvas quirk. Affects BOTH the in-chat inline chart and the gallery popup on mobile. Fix: ensure non-zero container size before embed (or defer embed to post-layout), try SVG renderer, verify vega-embed7/vega6 compat; CONFIRM whether desktop is affected (user hasn't checked). Web batch.
- **J2/F2/F3 RE-CONFIRMED (user 2026-06-16):** reports/charts get their own dedicated section UNDER the Layers panel for inspection (in addition to the working in-chat inline chart). Already queued as J2; user re-affirmed after the Asheville run.
- **F38 (engine):** hillshade transparent over water/flat areas — as a TOOL PARAMETER, not hardcoded (user: "could vary depending on use case"). Over ocean/lakes (flat, even DEM) hillshade is uniform-gray noise; often you want it transparent so only land terrain shows, but sometimes you want full coverage — so the LLM decides per use case (Invariant 2). Add a param to `compute_hillshade` (e.g. `mask_flat_water: bool` or `transparency: "none"|"water"|"flat"|"both"`), surfaced in the catalog so the agent sets it from user intent. Implementation of the mask: alpha=transparent where DEM nodata OR slope ~= 0 OR NLCD water mask. Sensible default (likely mask-on for terrain focus) but agent-overridable. Same optional param pattern for colored_relief / shaded_relief (F37). DEM-derived layers only.
- **F39 (engine/agent):** CLIP every overlay to the AOI bbox before publish. User: tools return extents LARGER than the bbox (tile-snapped DEM COGs, wide road/river fetches, NLDI traces) — only the bbox is wanted, for clean visibility. Fix: each fetch/compute tool clips its output to the EXACT requested bbox before returning/publishing — rasters via `clip_raster_to_bbox` (DEM, hillshade, relief, landcover, flood), vectors via `clip_vector_to_polygon`/bbox (roads, rivers, and all future overlays). Deterministic clip in the tool path (preferred) over relying on the LLM to call a clip tool. General rule for ALL current + future overlays. Relates to [[feedback_geographic_clipping_pattern]] (bbox variant of the admin-polygon clip).
- **F37 (engine) -> job-0319:** server-side raster BLEND/bake tool (realizes F37 + the landcover-x-hillshade ask). User asked twice (gradient/relief x hillshade; land cover x hillshade) and the agent PUNTED the blend to the client ("set multiply blend mode") — which MapLibre GL cannot do. Build ONE general `compute_blended_composite(base_layer_uri, overlay_layer_uri, blend_mode="multiply")` that reads both COGs, aligns to a common grid, applies the blend (reuse compute_hillshade swiss_double multiply math), and writes ONE composite RGB COG (tiled+overviews, clipped to overlap) -> publish as a single baked layer. @register_tool decorator pattern (self-registers). Catalog description must steer the agent to use it for "bake/blend/combine/drape layers" and NEVER tell the user to set client-side blend. LIVE 2026-06-16: user asked for a gradient + hillshade combined; agent rendered them as TWO SEPARATE layers and the opacity toggle washes out rather than blends. Root cause: MapLibre GL has NO per-layer multiply/overlay blend mode, so stacking + opacity can't produce crisp shaded relief. Fix = bake server-side into ONE COG: hillshade multiply-blended over colored relief (single `compute_shaded_relief` engine tool, or a compose step) so "shaded/gradient hillshade" returns one baked layer. This is the F24 blending use-case realized server-side. Engine job; pairs with F35 contours + F36 3D.
- **F35 (engine):** terrain contour / elevation lines. No contour tool today. Add `compute_contours` (gdal_contour from the DEM COG -> vector contour LineStrings at an auto/again interval) that renders inline like roads/rivers. Small, quicker win; can land before the 3D job.
- **F36 (web+infra, DEFERRABLE -> own job):** 3D elevation view with layers baked in. User goal from the beginning. KEY CORRECTION: in-browser interactive 3D is **MapLibre terrain (`setTerrain`)**, NOT QGIS Server (QGIS 3D is desktop-only; QGIS Server does WMS/WCS/processing, not live browser 3D). We already have the pieces: DEM COGs + TiTiler. Path: TiTiler serves terrain-RGB tiles from the DEM -> MapLibre `raster-dem` source + `setTerrain` -> 3D terrain with existing layers (relief/flood/roads/rivers) draping automatically -> two-finger pitch/rotate is built in -> constrain DEM to the AOI so 3D is bbox-only, exactly as described. Contained feature (a job, not a new subsystem). Sequence: after floods + MODFLOW are solid, or pull forward as a "quickish win" if stuck on them.
- **F33 (engine):** raster COGs must have OVERVIEWS or they render spotty. LIVE DIAG 2026-06-16 (Chattanooga land cover): `fetch_landcover` (MRLC NLCD WCS) publishes a 694x557 COG with `overviews: []` -> TiTiler 404s all below-native-zoom tiles -> layer is patchy/missing when zoomed out, fills only near native zoom. TiTiler itself is HEALTHY (NRestarts=0, hardening held; in-area native tiles serve 200 in ~20ms). Fix: route raster-fetcher output through a COG translate WITH overviews (the `_translate_to_cog` path compute_colored_relief already uses) so `fetch_landcover` (and any WCS/raster fetcher) emits tiled+overview COGs; PLUS harden `publish_layer` to validate overviews and auto-generate them (rio-cogeo cog_translate) before registering the tile template — a general defense so NO raster source can publish a no-overview COG. Bundle with the agent deploy. NOT a TiTiler-wedge recurrence.
- **F32 (agent) -> job-0316:** kill the false "Publishing layer... failed" card on vectors. The agent calls `publish_layer` on every vector (roads, rivers); publish_layer correctly rejects vectors, but this paints a scary RED failed card on a SUCCESSFUL inline render. Fix: (a) remove the publish_layer hint from vector-fetch tool docstrings (the NLDI docstring literally tells it to publish); (b) strengthen the system prompt rule "NEVER publish_layer a vector — it already rendered"; (c) make the vector rejection a benign result, not a failure card. Affects roads too.

## SRS-amendment-worthy (design decisions, not just bugfixes) — propose + land with NATE's go
- Resize-by-drag replaces panel-expansion (FR-WC / FR-MP-6 Case UX).
- New Visualizations/Reports panel + persisted-report popup pattern (new FR-WC requirement; reports are first-class alongside layers).
- Legend bundled-with-layer lifecycle (FR-WC).
- No-emoji narration (Appendix I harness / FR-AS narration discipline).

## Overlap / risk notes
J1→J2→J3 all touch the two-pane shell + LayerPanel → **strictly sequential**, re-verify the panel after each. J4/J5/J6 are isolated and safe to interleave. Re-demo after Stage 2 (the headline: report persistence) rather than waiting for all stages.
