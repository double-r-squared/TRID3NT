# Cloud Function runtime dependencies (Python 3.12) — job-0251.
#
# These are the DEPLOYED function's runtime deps. They are imported LAZILY
# (inside the function bodies in main.py) so the pure-Python unit tests in
# test_mint_signed_url.py run WITHOUT installing any of them (the tests inject
# fakes via the _DEPS hook). Pins are conservative current-major lines as of
# 2026; bump in lockstep with a re-run of the unit suite + a redeploy.

functions-framework==3.*        # HTTPS entry-point harness (Cloud Functions gen2)
firebase-admin==6.*             # Firebase ID-token verification (auth.verify_id_token)
google-cloud-storage==2.*       # GCS blob.generate_signed_url (V4)
google-cloud-secret-manager==2.*  # Atlas SRV fetch (never hardcoded)
google-auth==2.*                # IAM signBlob signer (no key file — manifest constraint)
pymongo==4.*                    # direct Atlas read of the Case owner (documented divergence)
