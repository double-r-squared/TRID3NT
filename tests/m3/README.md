# M3 acceptance suite (job-0028)

Run once on a fresh box: `make playwright-install` (downloads Chromium + Firefox-ESR to `~/.cache/ms-playwright/`). Then `make test-m3` to execute the suite — `.venv-agent`'s `python -m pytest tests/m3 -v --tb=short` against a real Vite dev server + the deployed Cloud Run QGIS Server (`@sha256:57d0f43`, post job-0029 CORS fix). The agent does not yet emit populated `session-state.loaded_layers` or `pipeline-state` envelopes in M3 (M4 work); the LayerPanel + PipelineStrip tests therefore seed component state through the `window.__grace2Inject*` dev seam (an internal seam, not an external boundary — surfaced in the report's Open Questions). The cancel-envelope test rides the real M1 cancel chain (job-0015 verified end-to-end at 502 ms). Cross-browser split: the two visual smoke tests (`test_wms_tiles.py` + `test_layer_panel.py` initial / after-state captures) parametrize over Chromium + Firefox-ESR (FR-WC-1); the pipeline-strip, screenshot-smoke, latency, camera-lock, and gs-uri tests run Chromium-only — Firefox-ESR's cross-browser proof is satisfied by the visual smokes covering the layout shell.
