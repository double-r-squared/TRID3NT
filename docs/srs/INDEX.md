# SRS v0.3 — Section Index

This directory is the **canonical editing surface** for the GRACE-2 Software Requirements Specification. Each file below maps to one section or appendix of the SRS; together, concatenated in the order shown, they reproduce `docs/SRS_v0.3.md` byte-for-byte (the monolith is regenerated by `make srs` and preserved only for backward compatibility with the immutable line-references in `reports/complete/`). Sub-agents (specialist runners, reviewers, orchestrator audit subagents) should `Read` the narrow file for the section they need rather than loading the full monolith — load `docs/srs/03-functional-requirements.md` for an `FR-XX-N` citation, `docs/srs/B-assessment-envelope-schema.md` for envelope work, `docs/srs/07-milestones.md` + `docs/srs/02-system-overview.md` for sprint planning, and so on. All amendments land in the narrow files; the user runs `make srs` to regenerate the monolith.

| Section | File | Purpose |
| --- | --- | --- |
| Preamble | [`00-preamble.md`](00-preamble.md) | Title, version (v0.3.20), authors, supersedes note, opening hr separator. Loaded only by tools regenerating the monolith. |
| §1 Purpose and Scope | [`01-purpose-and-scope.md`](01-purpose-and-scope.md) | What GRACE-2 is, audience, scope boundaries. Load when reasoning about v0.1 vs deferred-future scope. |
| §2 System Overview | [`02-system-overview.md`](02-system-overview.md) | Decisions A–O (§2.1; Decision O caches-mediated data fetching added in v0.3.15), architecture diagram (§2.2), engine contract `(location, forcing) → AssessmentEnvelope` (§2.3) with 8 new Deferred engine rows added in v0.3.15. The invariant substrate. Load for any cross-cutting architecture work; orchestrator-pinned invariants derive from §2.1. |
| §3 Functional Requirements | [`03-functional-requirements.md`](03-functional-requirements.md) | All ten FR-XX groups (FR-WC, FR-AS, FR-TA, FR-HEP, FR-QS, FR-PHC, FR-DT, FR-MP, FR-CE, FR-DC). FR-DC (data caching, six labeled requirements) and new §3.9 added in v0.3.15. Kept as one file: no single FR group exceeds 120 lines after the amendment, cross-references are dense, splitting would fragment tightly-coupled material. Load when authoring/reviewing any specialist work (every kickoff cites FR-XX-N anchors). |
| §4 Non-Functional Requirements | [`04-non-functional-requirements.md`](04-non-functional-requirements.md) | NFR — Performance, Reliability, Security, Cost, Compliance/Licensing, Portability/OSI posture. Load for SLO/SLI work, infra cost ceilings, OSI licensing checks. |
| §5 Out-of-Scope (v0.1) | [`05-out-of-scope.md`](05-out-of-scope.md) | Deferred futures (multi-LLM, 3D, multi-tenant auth, mobile, etc.). Load to defend a 'deferred' verdict on a feature request. |
| §6 Open Questions | [`06-open-questions.md`](06-open-questions.md) | OQ-1..OQ-11 (OQ-5 closed inline by Decision O in v0.3.15; OQ-10 reserved with explanatory parenthetical; OQ-11 conservation/biodiversity domain SRS position added in v0.3.15; specialist-surfaced OQ-* additions tracked in `PROJECT_STATE.md`). Load when resolving an OQ or proposing a new one. |
| §7 Milestones | [`07-milestones.md`](07-milestones.md) | M1–M11 (dependency-sequenced). Load when planning a sprint or translating milestones into sprint manifests. |
| §8 Document History | [`08-document-history.md`](08-document-history.md) | Every revision row v0.1 → v0.3.15. Load only when authoring an amendment landing row. |
| Appendix A: WebSocket Protocol | [`A-websocket-protocol.md`](A-websocket-protocol.md) | Envelope shape (A.1), URL/version negotiation (A.2), client→server messages (A.3), server→client messages (A.4 / A.4b), replace-not-reconcile semantics (A.7), SCREAMING_SNAKE_CASE error codes (A.6). Owned by `schema`. Load when amending the protocol or implementing client/server message handlers. |
| Appendix B: AssessmentEnvelope | [`B-assessment-envelope-schema.md`](B-assessment-envelope-schema.md) | The central FR-TA-1 output (B.2), supporting types (B.3), Flood subtype (B.4), discovery payloads as permissive dict (B.6b), Pelicun impact extension (B.6c/d). Owned by `schema`. Load for envelope amendments and engine output shape work. |
| Appendix C: EventMetadata | [`C-event-metadata-schema.md`](C-event-metadata-schema.md) | Real-world hazard event representation (C.2), ClaimSet/NumericClaim multi-source numerical evidence (C.3, Decision M), intensity discriminated union by event_type (C.4), event_type vocabulary (C.7). Owned by `schema`, sourced by `engine` (hazard event pipeline). Load when working on FR-HEP, claim aggregation, or intensity payloads. |
| Appendix D: MongoDB Collection Schemas | [`D-mongodb-collection-schemas.md`](D-mongodb-collection-schemas.md) | Six collections (sessions, runs, projects, events, layers, sessionrecords), vector index configs (Decision L, OQ-7), TTL config, ProjectLayerSummary (D.2), PipelineSnapshot/PipelineStepSummary (D.6), session-records carveout. Owned by `schema`. Load when amending collection shape, indexes, or FR-MP-5. |
| Appendix E: QGIS Plugins Inventory | [`E-qgis-plugins-inventory.md`](E-qgis-plugins-inventory.md) | Forward-looking catalog (added in v0.3.15) of QGIS plugins for the deferred engines in §2.3 (Q4TS for TELEMAC, QGIS-HMS for HEC-HMS, FREEWAT/QSWATMOD/iMOD/APEXMOD for the MODFLOW family, THYRSIS for hydrogeology) and the conservation/biodiversity utilities in §3.3 FR-TA-2 (Maxent/QMaxent, InVEST, Circuitscape-QGIS, LinkScape/TerraLink, Animove, Trajectools, LecoS). Three-tier image-bake strategy (QGIS Server / PyQGIS worker / dedicated solver containers). Informational: a plugin appearing here does NOT bind the architecture to invoke it through QGIS Processing. Load when planning plugin-side integrations or deciding between plugin-backed vs Python-shim modes for a deferred engine. |
| Appendix F: Data-Source Tiering + Access Patterns + Discovery + Secrets UX | [`F-data-sources-discovery-secrets.md`](F-data-sources-discovery-secrets.md) | Forward-looking (added in v0.3.16; §F.1.1 added in v0.3.17). **§F.1**: 3-tier ranking by credential requirement (Tier 1 key-free public, Tier 2 key-required free, Tier 3 paid commercial); Tier-1-preference rule. **WorldPop is the `fetch_population` Tier-1 default** (replaces ACS); ACS opt-in via `dataset="acs_2022"`. **§F.1.1 (v0.3.17): Access-pattern tiering** — orthogonal axis to credential tiering. 4 tiers (Tier 1 STAC+COG, Tier 2 OGC service, Tier 3 direct HTTPS+Range, Tier 4 region download + local clip with two-stage cache). Engineer-curated at implementation time after live provider verification; runtime fallback deferred (OQ-AT-1). Forward-looking v0.2+ capability: agent-mediated data-source discovery + adaptation (OQ-AT-2) — the "data stores in the wild" principle: agent encounters arbitrary providers, probes their access pattern, registers dynamically. Mirrors FR-AS-9 solver-discovery applied to data. **§F.2 Discovery-First lane**: catalog-driven `show_hazard_layer` for FEMA NFHL, USFS WHP, USDA WRC, NOAA SLOSH, USGS NSHM, NOAA Storm Events, USGS Quake, NOAA CDO (Tier 2 access per §F.1.1). **§F.3 Deferred Secrets UX**: forward-looking `request_secret` + Appendix-A envelope amendments + pop-up modal + Cloud Function out-of-band + per-user Secret Manager. DEFERRED INDEFINITELY. Load when planning data-source defaults, deciding access patterns, planning Discovery vs Modeling routing, or the M6+ secrets UX. |
| Appendix H: Authentication and Users | [`H-auth-and-users.md`](H-auth-and-users.md) | Forward-looking (v0.3.22 amendment, **decoupled** appendix per user direction 2026-06-08 — identity is a transverse concern). **H.1 Identity provider**: Firebase Authentication (managed SaaS, GCP-native, scales to GCP Identity Platform enterprise SKU without re-keying). Decision P. Anonymous-friendly by construction; custom-claims for tier gating; decoupled from compute. **H.2 User→Case ownership**: every Case carries `owner_user_id` (ULID); MongoDB MCP layer enforces ownership filter at query time (not in the agent prompt); v0.1 single-owner; collaborators / org-scoped / publish-mode deferred. **H.3 Anonymous→authenticated upgrade**: Firebase `linkWithCredential` preserves `uid` so anonymous Case ownership survives the upgrade; upgrade prompt fires at first share / first destructive action / explicit user action; satisfies "inform user before persistent action." **H.4 Custom claims for tier**: `tier: "free"|"pro"|"enterprise"` on the JWT; v0.1 all users are `free`; gating machinery in place, no gates armed yet; enterprise SKU adds `organization_id` / `roles[]` / `permissions[]`. **H.5 Session validation**: agent's WebSocket connect verifies Firebase ID token via `firebase_admin.auth.verify_id_token`; resolves `firebase_uid` → `users._id` via `Persistence.get_user_by_firebase_uid` (job-0115 substrate); token refresh + revocation + account deletion contracts pinned. **H.6 Secrets scoping**: `SecretRecord.user_id` + `SecretRecord.case_id` (None = user-wide); Tier-2 keys (eBird, IUCN, Movebank, Census/per-user) namespaced at `users/<user_id>/secrets/...` in Secret Manager; wire-level isolation per Decision F. **H.7 Decision P**: records the Firebase Auth selection with one-paragraph rationale + alternatives considered. Load when planning Auth substrate (Wave 2 schema D.x `users` collection, Appendix A token-on-connect handshake, A.6 error codes, agent-side verification middleware, web sign-in UX, Identity Platform IaC). |
| Appendix I: LLM Tool Harness Conventions | [`I-llm-tool-harness.md`](I-llm-tool-harness.md) | Decision F harness conventions, adopted 2026-06-08 per user direction "we can now tighten the harness" — binding from sprint-12-mega Wave 4.7 forward (job-0164 engine sweep + job-0165 this appendix). **§I.1 Param naming**: full unabbreviated words (`return_period_years`, `duration_hours`); the two v0.1 abbreviations `_yr` and `_hr` retain bounded backward-compat aliases until v0.2. **§I.2 `**_extra_ignored` absorb policy**: every `@register_tool` function ends with `**_extra_ignored: Any` so frontier-LLM invented kwargs (`run_name`, `scenario_id`, `description`, `mode`, ...) do not raise `TypeError`; the absorb is *input*-side noise tolerance and does not weaken Invariants 1 / 7 / 10. **§I.3 Docstring discipline**: dedicated `Examples:` doctest block at the end of every tool docstring; no inline `key="value"` substrings in prose `Use this when:` / `Do NOT use this for:` / `Params:` sections (Gemini-3 re-emits them as invented call arguments — empirically observed root cause of `forcing=` / `rainfall_event=` failures). **§I.4 Normalization layer** (`services/agent/src/grace2_agent/tool_arg_normalizer.py` — sibling job-0164): centralized `normalize_args(tool_name, raw_args) → (normalized, dropped_unknowns)` that runs in `server.py:_invoke_tool_via_emitter` before `entry.fn(**params)`; performs alias map + bounded fuzzy match (camelCase→snake_case, edit distance ≤ 1) + per-tool string-form parser (e.g. `forcing="atlas14_100yr"` → `return_period_years=100`) + drop-and-log unknown keys. Never raises on unknown kwargs; never silently fabricates values. **§I.5 Per-tool tests**: cross-cutting parametrized conformance test (every registered tool accepts invented kwargs without raising; docstring conforms; signature ends with `**_extra_ignored`) + property-based fuzz on the normalizer. Owned by `testing`. **§I.6 Decision F (harness conventions)**: records the 2026-06-08 adoption with rationale (empirical signal — strict signature binding was the #1 pre-application-logic failure across ~57 atomic tools) and alternatives considered (strict + re-prompt; pydantic-model-per-tool; normalizer without absorb). Owned by `schema`; consumed by `agent` (normalizer + server wiring), `engine` (tool conformance), `testing` (conformance suite). Load when amending the harness, adding a new atomic tool, registering a workflow exposure, or refining the normalizer surface. |

## Loading discipline

**Default:** load the narrow file. Sub-agents should `Read docs/srs/<NN-section>.md` for the section they need, **not** `docs/SRS_v0.3.md`. The narrow files are the canonical editing surface; the monolith is a regenerated convenience.

**When to load a narrow file (the common case):**

- A kickoff cites `FR-WC-4` → load `docs/srs/03-functional-requirements.md` only.
- Schema work on the envelope → load `docs/srs/B-assessment-envelope-schema.md` (plus `02-system-overview.md` if Decision G/N context is needed).
- Orchestrator translating a milestone into a sprint → load `docs/srs/07-milestones.md` + `docs/srs/02-system-overview.md` (Decisions).
- Resolving an OQ → load `docs/srs/06-open-questions.md` + the relevant FR/Appendix file the OQ points at.
- An amendment proposal in a report → cite line-anchored paths in the narrow file (`docs/srs/03-functional-requirements.md` line 412).

**When to load the monolith (`docs/SRS_v0.3.md`) — rare:**

- Reading an immutable historical report under `reports/complete/` that references `docs/SRS_v0.3.md` line N. The monolith is regenerated to those exact line numbers, so historical references stay resolvable.
- Bulk grep / global search for a token across all sections at once (`grep -n FR-TA docs/SRS_v0.3.md`). Note: `grep -rn docs/srs/` works equally well for new searches and is preferred.
- Initial onboarding scan ("what is this project?") — though even here, `docs/srs/INDEX.md` + `01-purpose-and-scope.md` + `02-system-overview.md` is usually sufficient.

**Editing discipline:**

- **All** amendments land in `docs/srs/<NN-section>.md`, never in `docs/SRS_v0.3.md`. After landing, run `make srs` to regenerate the monolith.
- Specialists still **propose** amendments through reports — only the user lands them — but the user lands them in the narrow file now, not the monolith.
- The monolith is kept tracked for backward compatibility with the immutable `reports/complete/` history. CI may verify `make srs && git diff --exit-code docs/SRS_v0.3.md` on PRs to catch drift.

**Lossless guarantee:** the monolith is reproducible byte-for-byte by `cat`-concatenating the parts in INDEX order. No banner is injected into part files (that would break round-trip). The "this file is generated" notice lives in the Makefile recipe output and this INDEX only.
